# Custom health check message
PING_MESSAGE=pong

# Skip login/signup (local dev / preview only). Never deploy this setting.
# AUTH_DISABLED=1
# With this enabled, the app uses an anonymous local session. AI/provider
# connections still use the normal Connect Builder / BYOK onboarding flow, or
# an environment key such as ANTHROPIC_API_KEY or OPENAI_API_KEY.
