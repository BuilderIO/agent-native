# Custom health check message
PING_MESSAGE=pong

# Skip login/signup (local dev / preview only)
# AUTH_DISABLED=1
# Use this only for an account-free local preview. Never commit it in .env or deploy it.
