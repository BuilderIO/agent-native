# Custom health check message
PING_MESSAGE=pong

# Skip login/signup (local dev / preview only)
# AUTH_DISABLED=1
# For loopback development only. Never commit or deploy this setting.
# With AUTH_DISABLED=1, the workflow opens without an account; AI connections
# still use shared onboarding or ANTHROPIC_API_KEY / OPENAI_API_KEY.
