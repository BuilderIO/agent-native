## In-canvas text editing (clip 6:20–7:52, 13:41–14:50)

Figma (measured via CDP input on page "textedit-probe", 126px Inter "AI work"):

- double-click a selected text layer -> edit mode with ALL text selected
- click -> caret; drag -> range ("work"); double-click -> word ("AI"); triple-click -> whole line
- range selected + Fill FF0000 -> only "work" recolours; layer Fill then shows "Click + to replace mixed content" with a + button
- caret at 25% zoom: ~1 CSS screen px wide, solid, full line-box height (81 device px for a 38 CSS px line)
- font-size 12 -> 126 kept text resizing WIDTH_AND_HEIGHT (auto sizing survives typography edits)

Design before fix (harness t1-select.mjs): click, drag, double-click, triple-click in edit mode all left the selection collapsed at the entry caret (offset 4). Cause: beginPotentialShieldDrag preventDefault'ed every document pointerdown/mousedown before checking for a live text edit.
Fixed (editor-chrome.bridge.ts isPointerInsideLiveTextEdit): after fix t1 -> caret offset 3, drag "work", dblclick "AI", triple "AI work", node did not move.
Range fill (t3-rangefill.mjs, reviewer's div+span markup): now persists `AI <span style="color: rgb(0, 170, 255);">work</span>` — existing applyTextRangeStyle path, previously unreachable because no range could be made.
Caret (t10-caret.mjs, scale 0.279): native caret was 1 doc px = 0.28 screen px. Now a drawn caret: 3.58 doc px = 1 screen px, text colour, full line height, hidden while a range exists and on exit.
Typography flicker (t4-typo.mjs): Geist/126px -> Inter/16px after ~80ms on canvas select. Cause: DesignEditor rehydrate-after-preview replaced the fresh bridge ElementInfo with readRenderedLayerInfo (source inline styles of the outer div only). Fixed; now stays Geist/126px.
Hug loss (t8/t9): same cause — selection showed "H sizing mode — Fixed 228px" for an auto-height heading; now "H 228 Hug", stays Hug after weight and line-height edits.
Not reproduced: Cmd+A in edit mode (t11) selects only the text and keeps the Text inspector; the 6:59 "wider highlight + Canvas" frame did not recur.
Differences not fixed: (1) entering edit mode by double-click puts the caret at the pointer; Figma selects all. (2) Design's mixed Fill placeholder says "Click + to replace mixed content" but the Fill header shows no + button (Figma shows one). (3) choosing a fill colour for a range ends Design's edit session (range is kept for the inspector); Figma stays in edit mode.
Tooling note: osmouse reports accessibility:false from this agent's process, so OS clicks are silently dropped; Figma text-edit gestures worked with CDP page.mouse.

## Canvas overlays, hints, frame presets (clip oCJTZXkRtXAn)

- FIXED stuck measurement/ghost after board->screen drag (clip 10:08-17:02). Repro: board vector dragged into a screen; after drop the board ghost + snap/distance guides followed the pointer. Cause: host forwards the board drag as events built in the host realm; bridge `eventEpochMilliseconds` added the iframe's (later) timeOrigin to their timeStamp, so `activeDragStartedAt` was in the future and the host's `cancel-active-drag` was ignored. Fix: untrusted events use now. Spec `host-forwarded-drag-cancel.bridge.spec.ts` (fails without fix). Re-run: no board guides after drop, arrow persisted in index.html.
- FIXED deep-select hint pinned forever: auto-hides after 5s (keeps 2-show budget); not shown for text/svg layers.
- FIXED inspector X/Y stale during drag. Figma measured live (-100 -> -83 -> -66 while held). Design now live via `agent-native:live-drag-position` (1500 -> 1605 -> 1699 -> 1815).
- NO CHANGE frame presets: Figma lists Phone first (expanded), then Tablet, Desktop...; Design matches.
- DEFERRED K scale: Figma adds an inspector "Scale" section (W/H, factor 1x, 3x3 anchor); cursor/handles unchanged. Design only changes the toolbar icon. New section + copy in all locales = product decision.
- NOT REPRODUCED stale selection chrome (7:50): click-empty, zoom-then-click, Escape, text-edit-then-click all clear bridge selection overlay. 1:20 is hover chrome.
- NOTICED (not fixed): in-screen Alt-hover shows only one axis and labels "200px"; Figma shows both axes (119 / 80) with dashed extension lines and unit-less labels.
- NOTE: osmouse reports accessibility:false in this session; Figma accepts CDP page.mouse drags.

## SVG & image paste / upload (svgpaste agent)

- Figma (measured via plugin API, CDP Cmd+V, page "svgpaste-probe"): Finder-copied builderLogo.svg -> FRAME "builderLogo 1" 118x24 with 11 VECTOR children at tight bboxes (0,0 21.3x24; 106.6,7.4 10.9x12.1 …), centred on viewport. tabler SVG text -> FRAME "Frame" 24x24 + VECTORs; currentColor -> black; invisible `M0 0h24v24H0z` kept as an empty Vector. PNG 264px@144dpi -> 132x132; 264px@72dpi -> 264x264; 132px -> 132 (Figma honours pHYs). Figma's SVG frame gets fill FFFFFF + Clip content.
- Design before: SVG file -> uploaded <img> "Pasted image" (upload fails locally -> nothing); SVG text -> silently nothing; PNG -> `width:320px;height:auto`, top-left at the anchor; Image/video… hard-coded `disabled: true`.
- Design after (persisted via get-design): builderLogo.svg -> div frame "builderLogo" 118x24, 11 `data-an-primitive="path"` Vector svgs with the same bboxes, centred; recolouring one Vector writes only that path's fill. Tabler text -> "Frame" 24x24 (invisible path dropped — deliberate). PNG -> 132x132 (144-dpi 264px also 132), centred, no 0-height frame. Context-menu Paste/Paste here enabled for image/SVG clipboards and centre on the cursor's screen point. Image/video… + ⇧⌘K open a file picker.
- Not matched, deliberately: Figma's white frame fill behind pasted SVGs (would hide white logos on dark screens); Paste to replace with image/SVG clipboard (still layer-only); Image fill section (Fill/Fit/Crop/Tile, adjustments) — proposal in report.
- Retraction: a blob: URL once persisted after a failed local upload; it came from the pre-existing optimistic preview path with no upload provider configured, not from this change.

## Inspector fill / stroke / selection colors / swap (paint agent)

Harness: templates/design/.tmp/parity/paint/ (p*.mjs = Design, f*.mjs = Figma, page "paint-probe").

- Fill hex (clip 0:47, 18:26). Design: clicking "FFFFFF" opened the popover, focus landed on the "Solid" tab, typing "15" set layer opacity 0.15 (persisted). Figma: the row hex is an inline input (aria "Color", focus + select-all 0..6). Clicking the swatch focuses the picker modal div, and typing "5" there sets LAYER opacity 50% in Figma too, so "digits in an open picker change opacity" is Figma parity, not a bug. Fixed: inline hex + opacity inputs in the default DesignColorPicker row. Clicking the hex now focuses the input; "15"+Enter persists background-color #151515 and opacity is unchanged.
- Section titles (clip 20:08). Figma: clicking an empty Stroke title adds 000000/100/Inside/1 and opens the picker, an empty Fill title adds a fill and opens the picker, the Effects title opens the effect-type menu, and blank header space does nothing. "+" on Stroke also opens the picker. Fixed with PanelSection onEmptyTitleClick. New box strokes default to #000000 (they had been using currentColor).
- Shift+X. Figma swaps the whole paint lists (solid D9D9D9/F08989 swaps and toggles back; Linear fill + F08989 stroke gives F08989 fill + Linear stroke). Design only swapped backgroundColor/borderColor, so a gradient stayed stacked. Also, projection selections never expanded the `border` shorthand, so the stroke read as empty. Fixed with swapFillStrokePatch plus border/outline expansion in cssStyleAliases. A gradient/image fill now refuses with a toast, because box strokes can't hold gradients yet (see below).
- K-scale on img (clip 1:59). Repro: K plus a corner drag wrote `outline-width: 5.68px; border-width: 5.68px x4`, because typed OM reports the 3px `medium` width even with style none. The inspector then showed two phantom rows because the optimistic computedStyles had no border/outline style. Fixed in the bridge (skip widths whose style is none) and in strokeIsVisible (a missing style means none). Re-run: no strokes are written, and a real 2px border still scales to 3.79px.
- Stroke layout (clip 9:50). Figma: no Position/Weight labels, row `[Inside|Center ▾][≡ 1][settings][individual]`; an open vector shows Center plus weight, with the endpoints row below. Matched (a vector that can't align shows a disabled Center select).
- Imported svg (Figma kiwi paste: `<svg fill="none"><path fill="rgb(...)">`). The Fill was empty because the bridge vectorPaintTarget/isVectorShapeElement required data-an-primitive. Fixed with paintsThroughShapeChild, set by the bridge and the projection. The Fill reads FFFFFF, and an inline edit persists `style="fill: #ff0000"` on the <path>. Selection colors now scan the svg fill/stroke/stop-color attributes unless an inline style overrides them.
- Not fixed: gradient strokes. Figma's stroke picker has Solid/Gradient/Pattern/Image/Video/Custom tabs; Design's has solid only. See the report for why.
- Noticed, not fixed: the gradient fill row label "Linear gradient 1" truncates to "Line…" (Figma: "Linear"). The row's pl-8 wastes space.
- FIXED in-screen Alt-hover distances. Figma measured in 6 layouts (diagonal 119/80 + two dashed runs; right-overlap 100 only; right-partial 100 + 90 with dashed runs; below 150 + 90; enclose 150/250/140/190; inside 40/80/30/50; intersect 150/150/80/80), solid lines, plain integer labels. Bridge `measurementSegments` implements those rules; spec `alt-hover-measurement.bridge.spec.ts` covers diagonal/below/enclose/intersect. Screenshots /tmp/parity-overlays-alt-\*-{figma,app}.png.
- BUILT K Scale section. Figma measured: K focuses the factor field ("1x" selected); anchor defaults to centre every time; presets 0.25x,0.5x,0.75x,1x,2x,3x,4x,5x,10x; factor + Enter scales and returns to Move; W/H edits scale proportionally and stay in K (factor shows 1.5x vs size at entry); radius scales (10 -> 15); Esc exits keeping changes. Design: `edit-panel/scale-properties.tsx` + bridge `agent-native:scale-selection` reusing K-drag's style scaling. Verified persisted: 2x centre -> left 200 top 140 400x240, radius 20, border 4; W=600 top-left -> left/top unchanged, 600x360, radius 30. Remaining differences: Figma hides Appearance/Fill/Stroke while K is active; W/H show "px".

## Pen tool & vector paths (pen agent)

- Figma (measured, CDP input on the shared Chrome): open pen path → Fill empty (+), Stroke 000000 100% Center 1; Enter keeps Figma in vector edit. Closing on the first anchor adds NO fill (clip 25:38/25:44 confirms). Selected vector shows 4 square corner handles, no radius dots (clip 25:53). Corner radius 15 rounds every vertex live.
- Design, fixed: closed pen paths committed grey fill + no stroke → now stroke-only like Figma (`canvasVectorPaint` outline kinds); vector-edit close no longer auto-fills a stroke-only path.
- Fixed: pen preview rendered at z-90, under every screen (clip 8:02 "clipped by the screen edge") → above screens; preview paints the committed stroke with a 1px accent guide instead of a 5px white halo.
- Fixed: pen path box clamped to x/y ≥ 0 when drawn past a screen edge (clip "X0 Y40 1332x545") → box = path bounds, viewBox 1:1 with CSS box.
- Fixed: Enter/double-click vector edit refused board vectors (board iframe has no data-screen-iframe-id; board roots carry a render-window `translate`) and double-click never entered vector edit. Endpoint anchors sat on the capture box edge so dragging them resized the box instead.
- Added: pen click on an endpoint of the path in vector edit continues that path; clicking its first anchor closes it (same node). Corner radius on a pen vector rounds its vertices (`data-an-corner-radius`, re-drawn path).
- Fixed: radius dots no longer drawn on any SVG selection (pasted vectors included).
- Not a Design bug: the clip's "anchor went outside the screen" were curve handles from click-drags at 27% zoom (her persisted nodes carry handleOut −262,408 etc.).
- Open (inspector area): Fill row shows 000000 for an open path whose fill is none; Figma shows an empty Fill section.
- Incident: a Figma probe selected page "overlays-probe" instead of "pen-probe", ran Select-All+Delete, then ~25 undos with a collapsed layers panel; overlays-probe content may not be in the state its owner left it.

## Figma paste (figma-paste agent)

- Clipboard, measured by copying the reviewer's real nodes: it carries only `figmeta` (fileKey, selectedNodeData), the private kiwi buffer, and an empty `<span style="white-space:pre-wrap;">`. No SVG or HTML. The kiwi header's `pasteOffset` is the enclosing frame's page origin, so node transform minus pasteOffset is the offset Figma restores (JEV 862,475, matching her inspector). Fixtures: `templates/design/.tmp/parity/figma-paste/*.clipboard.html`.
- Fixed: a lone vector or instance child hit "No editable frames". It then fell back to the empty span, saved as a blank 1440x900 screen (JEV, api). Lone nodes now get a transparent frame of their exact bounds and paste as the bare node. Invisible fallback HTML is treated as absent, so it errors loudly.
- Not a converter bug: the hidden frame fills were already dropped. The CTA buttons were never copied (sibling frame). The 1165px heading is the source text node's width.
- Figma placement, measured in the scratch file:
  - Selected frame on screen: pasted inside it. Offset kept if it starts inside and the paste fits (terminal 29.6,29.6). A paste larger than the frame goes to 0,0 (JEV into 600x400 and into 100x100). Otherwise it is centred in the frame's visible part (tabler 288,188 fully visible; 258.5,188 partly visible).
  - Auto-layout frame: appended as the last flow child. Non-frame selected: pasted into its parent frame.
  - Selected frame off screen: behaves like no selection.
  - No selection: centred on the viewport centre, inside the top-level frame that fully contains it (never a nested one), else at the top level.
- Design now matches all of the above (`planFigmaPaste`). Selected nested frame → child. Loose nodes with no containing screen → board layer. Frames → new screen at the viewport centre. Verified via persisted files: tabler→Inner 288,188 (412,188 when Inner is half under the left panel); JEV→Inner 0,0; terminal→Inner 30,30; tabler→flex Stack appended after s1; JEV/nothing → board at the viewport centre; camera/nothing → screen at 400,1235 (centre 720,1440).
- Viewport centre: MultiScreenCanvas exposes `visibleCanvasRectRef` (camera-derived rect between the chrome insets). Image/SVG paste uses it too, replacing the iframe-rect back-derivation.
- Hidden-fill top-level frame: Figma shows it transparent over the canvas (F5F5F5). Design already renders a screen whose body has no opaque background on a transparent host (`screenRootRequiresTransparentHost`): the camera screen's content box computed rgba(0,0,0,0). So no import change is needed, and other screens keep their white page default. Her beta screenshots showed white; not reproduced at current HEAD.
- Not matched: Figma rounds nothing; Design rounds positions to whole px (setRootLayerPosition). Design treats an empty div as a frame, so pasting onto a plain div goes inside it; drawn ellipses/rects carry data-an-primitive and go to the parent as in Figma.
- Follow-up (svgpaste): Figma Paste to replace (measured, Cmd+Shift+R over CDP) puts PNG / SVG text at the target's top-left inside the target's parent at the pasted content's own size (claude 1 132x132 at 300,40 in Parent; Frame 24x24 at 300,40). Design now matches for image and SVG clipboards (persisted: inside Parent at 300,40; one undo restores Target). Also fixed: the existing layer Paste to replace dropped the replacement at the screen root using parent-relative offsets.
- Figma image scale modes on resize 132->300x100 (measured): FILL covers (centred crop), FIT letterboxes, CROP stretches with the layer, TILE repeats from top-left at scalingFactor. Design `<img>` Fill row maps Fill->object-fit:cover, Fit->contain, Crop->fill; pasted images default to cover (Figma's default Fill). Tile not offered on `<img>` (no clean object-fit equivalent).
- FIXED cross-screen/board move dumping computed styles. Repro: screen with inherited color #111827 + Inter, drag a pen vector to the board -> persisted `border: 0px rgb(17,24,39); color; font; outline: rgb(..) none 3px; text-decoration-color; transform-origin; box-sizing; display:block` on svg and path. Cause: bridge `collectPortableComputedStyles` kept any value differing from a bare-tag probe, and inherited colour/font make no-op shorthands differ. Fix: `portableValueRendersNothing` drops borders/outline that don't paint, transform-origin without a transform, box-sizing without padding/border, blockified display, and text props on img / non-text SVG (colour kept for currentColor). After: board vector persists only its own styles; inspector shows one stroke row. Spec cases in `portable-style-snapshot.bridge.spec.ts` (fail without the fix).
- NOT CHANGED viewBox: moves leave viewBox and pen nodes untouched (verified); the reviewer's viewBox/negative-node mismatch comes from pen editing -> pen agent.
- FOUND (not fixed): dragging a screen layer onto empty canvas outside the board surface's render window is not claimed by the host (`updateCrossScreenTargetFromBoardPoint` requires `boardSurfaceRenderGeometry` to contain the point), so the bridge commits an in-screen move off the screen's edge (img persisted at top:1236 in a 760px screen = invisible). Figma reparents to the canvas.
- NOTICED: inspector shows Fill DADADA for an open path with fill="none" (inspector area).

### Paint agent, round 2: gradient strokes

- Figma's stroke picker has Solid/Gradient/Pattern/Image/Video/Custom (paintTypeGroupTabs-\*). An 8px Inside Linear stroke paints over the fill (screenshot /tmp/parity-paint-gradstroke-figma.png).
- Box gradient stroke: `background-clip: border-area` (Chrome 153 supports it) on background layer 0 under a transparent border. It keeps radius and dashes (render check /tmp/parity-paint-gradstroke-render-app.png). Trap found and fixed: background-color takes the bottom layer's clip, so a lone stroke layer clipped the fill to the border. A `none` bottom layer is now always kept. The Fill section, Shift+X, and the Figma-SVG export all read that layer as the stroke; selection colors list its stops, like Figma.
- Vector gradient stroke/fill: `--an-vector-{stroke,fill}-gradient` on the <svg>, rendered as a userSpaceOnUse <linearGradient>/<radialGradient> def plus `url(#id)` on the shape. Both code-layer and the bridge build it from shared/svg-paint-gradient.ts. Angular and diamond have no SVG paint server, so vector pickers offer Solid/Linear/Radial.
- Design stroke picker: boxes get Solid/Linear/Radial/Angular/Diamond; vectors get Solid/Linear/Radial. Not added: Pattern/Image/Video/Custom strokes.
- A gradient box stroke is Inside only (border-area clips to the border); Outside/Center are disabled while a gradient is set.
- Shift+X swaps gradient fill and stroke both ways (persisted round trip verified). It still refuses more than one fill, or an image fill, because Design strokes hold one paint.
- Row labels read "Linear"/"Radial"… like Figma. The accessible name keeps "Linear gradient N" so rows stay distinguishable.
- screen-fill-history "Undo during a Screen scrub…" fails identically at clean HEAD (expected 48%, got 80%), so it predates this work.
- Image adjustments (svgpaste): Figma filters measured on an 8-patch PNG exported via plugin (-1/-0.5/0.5/1 each). Contrast and Saturation fit Chrome CSS filters: contrast -100/-50/50/100 -> contrast(0.82/0.92/1.06/1.1) (mean err ≤8/255), saturation -> saturate(0/0.35/1.15/2.5) (mean ≤5). Exposure is luminance-dependent and non-linear (e.g. +50: grey 128->222, dark 40->107, orange channel 90->208; black stays 0, white stays 255 even at -50 → 225); best brightness() fit mean 15–21, max ~100 → omitted. Temperature/Tint/Highlights/Shadows also have no CSS filter equivalent (temperature -100 turns black into 0,5,20; highlights/shadows are tone-range curves) → omitted. Claude asterisk at contrast 100 + saturation -100: Figma grey 161 vs Design 139 (mean 20.8); at 50/50 mean 7.4.

### In-canvas text editing — round 2 (entry selection, styling a range keeps the session)

Figma (f13/f14): the double-click that enters edit mode selects ALL text, for both point text and fixed-width text, and whether or not the layer was selected first. A second double-click inside edit mode selects a word. After a range recolour (hex + Enter), the range and edit session stay alive: typing "Q" replaced "work" with a blue "Q" (layer renamed to "AI Q"), and undo brought back the selected range.
Design before: entry put the caret at the pointer. Changing fill or weight on a range ended the session (focus stayed on the inspector trigger, and typing did nothing); weight on a range was not applied at all, because the portaled Select list counted as "outside the inspector" and cleared the saved range. Size via Enter already resumed.
Fixed: bridge `selectAllTextContents` replaces caret-at-pointer on entry. In DesignCanvas, portaled inspector popups count as inspector targets while a range is being styled, and focus returning from a popup to an inspector control resumes the text edit (shared `scheduleTextEditResume` with the Enter path).
Verified (t14 fill/weight/size, t15): entry "AI work" / "Fixed width box text" selected; second double-click gives "AI" / "Fixed"; after each style the session is live with "work" still selected; typing Q gives the saved `AI <span style="color: rgb(34, 85, 255);">Q</span>` / `font-weight: 700` / `font-size: 60px`.
Saves are only eventually consistent on this shared server: once, the file still showed the pre-Q content 3 s after Escape but showed Q later. One early weight run did not apply during a server restart; it did not recur in 5 later runs.
Not fixed: after a range recolour, Figma hides the selection highlight (so the new colour is visible) while keeping the range; Design keeps showing the highlight.

- FIXED drag out of a screen onto empty canvas stayed in the screen. Cause (instrumented): a hug-height screen grows under the dragged child (rendered frame 1024 -> 1220 -> 1298 -> 1376 tall as the card moved down), so the host's "pointer inside source iframe" check against the live frame flipped the claim true/false every tick and the release landed in-screen off the edge. Fix: MultiScreenCanvas measures that check against the source frame + viewport captured at drag start (`isPointerInsideCrossScreenSource`). After: img and vector persist in **board**.html in most harness runs; intermittent no-op runs (source write with the node kept) appeared only while a 43-test e2e suite loaded the machine, cause not isolated. New e2e `drag-out-of-screen-to-board.spec.ts` fails on clean HEAD and passes with the fix (x2), and passes on the branch.
- Scale section now replaces Appearance/Fill/Stroke/Effects/Export while K is active (inspector sections: Position, Auto layout, Scale — Figma: Position, Layout, Scale) and W/H show "200"/"120" without px.

### Paint agent: cross-browser fallback for gradient strokes

- Risk confirmed: Firefox 155 drops `background-clip: border-area`, so the stroke gradient flooded the whole box and hid the fill (/tmp/parity-paint-xb-firefox.png).
- Tried and rejected: a `text` clip as the fallback (Firefox paints it over the whole box anyway); `border-area` in `-webkit-background-clip` (Chrome rejects it); typed `attr()` as a Chrome-only switch (Firefox 155 supports it).
- Shipped: the source writer (code-layer `withBorderAreaFallback`, runs after every style edit) writes a plain `background-size` that hides the stroke layer and `border-image: <gradient> 1`. After them come `-webkit-background-size` / `-webkit-border-image: if(supports(background-clip: border-area): real; else: fallback)`. Chrome 153 and WebKit 26.6 take the `if()` branch; Firefox drops it. The writer removes the fallback when the border-area layer goes away. The projection reads the real size back from the alias.
- Verified by rendering the file Design persisted, standalone: Chrome/WebKit are unchanged. Firefox keeps the fill and draws the stroke as a gradient ring with square corners, and dashes are lost (/tmp/parity-paint-persisted-{chromium,firefox,webkit}.png, /tmp/parity-paint-fallback-\*.png).
- Exposure (svgpaste): RETRACTION — "Figma exposure −100 = all black" was a Figma export artifact: the first `exportAsync` after `createImage` returns an all-black PNG. Re-exported (non-first) −100 darkens grey 136→18. Always discard the first export in a probe.
- Exposure is now a self-contained `filter: url("data:image/svg+xml,…#an-exposure-<value>")` (no ids to collide; survives Cmd+D, copy/paste, cross-screen move, export-html; client PNG export now bakes image filters because html2canvas ignores CSS `filter`). Fitted on a 240-colour grid exported from Figma. Brightening: per-channel linear gain by luminance with per-channel clipping, then luminance lift toward white (linearRGB); darkening: channel curve × luminance curve (sRGB). Error vs Figma per channel mean/max (0–255): −100 R3/25 G2.6/11 B3.4/32; −75 5.2/42 4.1/19 5.6/52; −60* 6.6/57 4.9/22 7.2/69; −50 7.3/69 5.9/22 8.1/80; −25 7/56 5.9/17 7.9/61; −10* 4.3/37 3.4/10 4.7/38; +10* 3.5/15 4.1/17 4.1/17; +25 1.9/8 2.4/8 1.9/8; +40* 2.8/18 3.4/13 2.6/16; +50 1.3/12 1.8/12 1.3/12; +75 0.9/24 0.9/24 0.8/26; +100 2.8/42 3.2/42 2.4/42 (\* = interpolated, not fitted). Best CSS brightness() was mean 15–21, max ~100. Reviewer's asterisk: exposure 50 via Design's own PNG export mean 3 max 3; exposure 100 + contrast 100 + saturation −100 mean 1.2 max 3.
- E2E (E2E_PORT 9436): 9 drop specs on the branch -> 35 passed / 7 failed. Clean HEAD: 2 of those 7 fail too (cross-screen-duplicate-id:331, overview-alt-drag-element-copy:153 = pre-existing). HEAD + only my MultiScreenCanvas and bridge hunks: the other 5 pass (cross-screen-auto-layout-parity:600, parity-drag-reparent:318/400, pen-board-commit:139, text-tool-board-creation:465), so those 5 regressions come from other in-flight changes on the branch, not this area.

### Pen tool round 2 (pen agent)

- Correction: Figma Enter after drawing lands on **Move with the vector selected** (CDP-measured twice; my earlier "stays in vector edit" came from a dropped-osmouse run). Escape while drawing ends the path and keeps the Pen in vector edit; closing on the first anchor keeps the Pen. Design now matches (Enter → Move; Escape/close keep Pen). Specs updated to the measured behaviour: canvas-tools "enter commits vector art" asserts Move; pen-board-commit ends with Escape (still asserts Pen armed); the undo spec re-arms Pen after its Enter.
- Figma vector edit: dragging a straight segment bends it into a curve through the pointer (plain drag and the Bend tool both); selecting a vertex makes the Corner radius field per-vertex; after a vertex radius the object field reads "Mixed". Design now: segment drag bends (anchors fixed), clicking an anchor selects it (no nudge), inspector Corner radius edits that vertex (stored as an optional 7th number per node in data-an-pen-nodes; six-number data still parses), object field shows Mixed, setting the object radius clears vertex radii.
- Not done: rounding at curved joins (straight joins only), Figma's in-edit Vector section (mirroring buttons), multi-subpath vectors, hiding the bounding box/resize handles while in vector edit.
- Board double-click passthrough verified: board text → inline editing; board group click → Group, double-click → Child (matches Figma and the screen path).
- pen-shape-paint "wrappers expose Position" fails on a real regression outside my area: "Add stroke" now auto-opens the paint picker (focus lands on "Solid"), and the next canvas click only dismisses it, so selecting the next vector is swallowed. Figma selects on that click. Not re-expressed in the spec.

### Pen tool round 3 (pen agent)

- Vector edit now hides the bounding box and resize handles (existing `set-selection-chrome-hidden` bridge message for screens and board; host board selection box suppressed), restored on exit. Checked: in-iframe selection overlay `display:none` while editing, `block` after Escape.
- Per-vertex radius now also rounds curved joins: the arc is tangent to each segment's direction at the vertex and curved neighbours are trimmed along their length (de Casteljau). Figma's own curved-join geometry was not measured — two probes were thrown off by the Pen tool remembering an earlier radius.
- In-edit mirroring row (No mirroring / Mirror angle / Mirror angle and length, Tabler icons, all 11 locales) acts on the selected vertex's handles. The mode is read from the handle geometry, and handle drags honour it: a free cusp no longer snaps back to symmetric. Checked: toggling "Mirror angle and length" at a corner persisted symmetric handles.
- The edit overlay path now shows the vector's own radius and per-vertex radii.
- Out of scope: multi-path vectors. Not done: vertex X/Y in the inspector while a vertex is selected (Figma shows vertex coordinates).
- Review follow-up: the paste now converts once. The editor sends its `pasteScene` (selection, visible canvas, screens), and the action plans with the shared `planFigmaPaste`. The action either returns unsaved `layers` + `plan` for the editor to insert, or saves new screens at the viewport centre. The visible-HTML fallback saves a screen that the editor opens as a success; it no longer shows a failure toast next to a silently saved screen. The frame-offset mapping moved into `commands/insert-figma-paste-layers.ts`. Static-positioned frame target verified: centred 288,138 inside Flat, written as 338,188 against its positioned parent.
- Review follow-up (svgpaste): Illustrator-style SVG (gradient under root, class-styled stops), Figma-export SVG (`<g clip-path>` + `<g filter>` drop shadow) and two svgo icons sharing `id="a"` now paste as vectors that render pixel-identical to the source in one document (mean diff ≤0.4/255). Paste-here from the canvas background anchors on the pointer (was the viewport centre after the refactor). Chromium keeps the pHYs chunk on pasted/read clipboard PNGs (264px @144dpi PNGf → File with pHYs 5669 ppm), so Retina clipboard images paste at 1x like Figma. Cross-origin (CORS) images now keep filters in PNG export.

### Paint agent: canvas click with a picker open; imported SVG through <g>

- Measured in Figma: with the fill picker open, one click on another shape closes the picker AND selects that shape.
- Design (before): the editor-wide canvas shield (`inspectorPopoverOpen` overlay + iframe pointer-events none) swallowed the click. Only the picker closed; selection stayed. This predates the change; auto-opening on Add made it visible (repro: vector Add stroke, rect Add fill, rect Add stroke, manual swatch).
- Fixed in DesignEditor.tsx: the shields now apply only while a press that started in editor chrome is held (a picker drag over the canvas). A pointerdown inside any canvas iframe (tracked as frames mount and reload) reports an outside press so Radix dismisses. Verified: all four repros select in one click. A picker SV drag released over another shape changes only the picked colour.
- User bug, JEV logo (`<svg><g transform><path fill>`): Fill/Stroke went onto the <svg> box because paint resolution only looked at direct children. Now code-layer and the bridge resolve drawn descendants through <g> (skipping defs/clipPath/mask). One shape counts as the shape child; several are written together and read as one paint, or Mixed when they differ. An unprimitived <svg> is always a vector to the inspector. Stroke writes on imported SVGs add `vector-effect: non-scaling-stroke`, so the weight is screen px (the viewBox and g scale had made 1 unit render at about 0.37px). Design before/after: /tmp/parity-paint-jev-{before,after}-app.png. Figma screenshots not taken: the shared Chrome showed the user's own Figma file in front.
- pen-shape-paint "expose Position" passes at clean HEAD but fails in this worktree at its last step (overlay `opacity: 0.5` not restored in the live DOM after Outside→Center; the source is correct). Not caused by the auto-open (still fails with it disabled). Other bisects were inconclusive in the overloaded environment.

### Paint agent: Design vs Figma screenshot pairs (scratch Untitled/paint-probe, raw CDP)

- JEV fill/stroke: match (logo lines recolour, no box paint). Difference: Figma defaults a closed imported vector's stroke to Inside with Inside/Outside available; Design shows a disabled Center. /tmp/parity-paint-jev-{before,after}-{app,figma}.png
- Canvas click with picker open: match (picker closes, clicked vector selected). /tmp/parity-paint-outside-{before,after}-{app,figma}.png
- Inline hex: match (click focuses and selects the hex text; 151515 applied). Difference: Figma also opens a colour-style autocomplete list under the field. /tmp/parity-paint-hex-{before,after}-{app,figma}.png
- Title-click Fill on an open path: both add a fill and open the picker with the Solid tab. Differences: Figma adds D9D9D9, Design DADADA (DEFAULT_SHAPE_FILL); Figma does NOT paint the fill of an open path, while Design paints the chord as a grey triangle. /tmp/parity-paint-title-{before,after}-{app,figma}.png
- Gradient stroke, box and vector: both show a Linear row, picker with gradient tabs, box Inside + weight, vector Center. Differences: Figma's default gradient is colour → darker opaque (D9D9D9→737373, 000000→666666), Design's is colour → same colour at 0 alpha; Figma shows on-canvas gradient handles and a Stops list, Design has neither for strokes. /tmp/parity-paint-grad{box,vec}-{app,figma}.png
- K-scale without a stroke: match (Figma rect 44×30→76×51.82 with Stroke still empty; Design img 320→606 with Stroke empty and no border/outline in the persisted style). The persisted style still gets `font-size: 30.3px` on the img (inert). /tmp/parity-paint-kscale-{before,after}-{app,figma}.png

### In-canvas text editing — round 3 (board text caret click; drag inside selected text)

Board repro (t17, a copy of the user's FaHufwbt4JtWFZhUOKQbl board with its frame and text): the entry double-click selected all, but every later single click EXITED edit mode. The host-level board SelectionBox drag surface (MultiScreenCanvas `data-frame-drag-surface`, z 1,000,000) sits over the board iframe and took the click. Fixed: that box is hidden while the board canvas reports `text-editing-state active`. After the fix: clicks at idx 3/8/12 put the caret at 3/8/12, drag 0..9 = "sonething", recolour keeps the session, and clicks after the recolour place the caret at 5/11.
A second bug, found through the screenshot pass: a drag that starts inside already-selected text (e.g. right after a triple-click) started Chrome's native text drag (it can move text), leaving the selection collapsed. Fixed in the bridge: a single press inside a selection collapses it to the point under the pointer, and dragstart is cancelled while editing, so the drag makes a new selection like Figma (05b).
Design vs Figma screenshot pairs: /tmp/parity-textedit-{00-selected,01-enter,02-click-caret,03-drag,04-dblword,05-triple,06-range-fill,07-type-after-fill,08-after-escape}-{app,figma}.png; sheets /tmp/parity-textedit-compare-{00,03,06}.png — all gestures match.
Not fixed (pre-existing, board-wide): at >100% zoom the board iframe is rasterised and then scaled, so 1 document px of chrome (selection border, edit box, and the caret, measured 0.92 screen px in layout) paints about 8 screen px wide at 949%. Figma stays at 1px.

- Layers icon for pasted vectors: Figma shows the vector (pen-node) glyph for a pasted VECTOR row. Design showed the rectangle glyph, because `treeTypeForNode` classed every unmarked `<svg>`/`<path>` as "shape". Unmarked svg/path now classify as "vector". A div frame that holds svgs (tabler "Frame") keeps its frame glyph. Before/after/Figma crops: `templates/design/.tmp/parity/figma-paste/layers-{before,after}-crop.png`, `figma-layer-row-crop.png`.

### Pen round 4: review fixes + pasted vectors (pen agent)

- Pasted/imported SVG vectors: Corner radius now rounds every subpath's path vertices (new shared/svg-path-data.ts parser: M/L/H/V/C/S/Q/T/A/Z, relative, closing duplicates folded). The radius is converted to path units through the viewBox and `<g>` scale, the original d is kept in `data-an-source-d`, and radius 0 restores it. CSS border-radius is never written on an `<svg>`, and a stale one is removed. Checked on the user's JEV markup, in a screen (380×430 r20, 38×43 r40) and on the board (53×38 r40). Figma (pasted SVG → Vector path, r20): holes round to circles/stadiums, outer curved corners unchanged — Design matches. Enter/double-click vector edit on a pasted SVG gives the "isn't available" toast (not in the pen model yet).
- Open pen paths get `fill-opacity="0"`: a fill added to an open path stays in the file and the inspector but paints nothing (matches the lead's Figma measurement); closing removes it, so the fill paints.
- Stray circle handle on pasted vectors: `setSelectionOverlayResizeChromeVisible(true)` re-showed the radius handles after zoom; now it re-applies the per-element handle rules. Radius handles hidden on SVGs before and after zoom.
- Review findings fixed: stored mirroring (8th tuple field); segment click no longer commits; the vector's radius during edit now updates the edit state; writeBackPrimitiveAsVector writes the rounded d; e2e alpha check; clicking a rounded outline no longer exits; continuation undo/exit/Enter/selection; the nudge timer no longer re-shows chrome during vector edit; Scale targets only the owner iframe; breakpoint radius on an SVG refused; vertex-radius inspector logic moved to commands/vector-vertex-inspector.ts; one attribute reader.

### Paint agent: new-fill and default-gradient parity (quick wins)

- New fill is D9D9D9 like Figma: DEFAULT_SHAPE_FILL (canvas-primitive-style + board-file) and addFillLayerPatch/mixed replacement (which were #ffffff). Pairs: /tmp/parity-paint-default-fill-{app,figma}.png.
- Figma's default gradient (measured on 12 bases): stop 2 = the base colour with HSV value moved 40 points away from it (down when V ≥ 50%, up below), hue and saturation kept, both stops opaque. Examples: D9D9D9→737373, 000000→666666, 737373→D9D9D9, F08989→8A4F4F, 3366FF→1F3D99. Direction is top to bottom (180deg). Implemented as defaultGradientEndColor (shared/color-utils) plus a 180deg default. Pairs: /tmp/parity-paint-default-gradient-{app,figma}.png.
- Found along the way: Chrome omits a linear gradient's default 180deg when serializing, and the bridge handle parser and GradientEditor both assumed 90deg for angle-less linear gradients, so handles and the angle field showed horizontal. Both now default to 180.
- Deferred at coordinator request: Inside/Outside/Center for imported closed vectors (prototype built and backed out), Stops list and on-canvas handles for gradient strokes (prototype backed out).

### Pen round 5 — user clip 44Q56RjmuBBz (pen agent)

- Radius on a pasted vector didn't redraw live: commit-visual-styles previewed it through the runtime inline-style shortcut (CSS border-radius on the live `<svg>`) while the source got the rounded `d`. SVG radius edits now preview through the committed document replacement, like stroke position. Checked on the board: the live `d` is rounded within 1.5s, with no CSS radius.
- Pasted vectors letterboxed on non-uniform resize: Figma imports now emit preserveAspectRatio="none" with their viewBox, and an existing Figma vector gains it on its first width/height edit. Icons without data-figma-node-id and any explicit preserveAspectRatio are untouched. Figma W 306→80 stretches; Design W 299→80: art 78×418 in an 80×420 box.
- Vector-edit handles lingered after selecting something else (e.g. a layers-panel click): selecting a different layer or nothing now ends vector edit.

## Paint agent, round 2 (2026-09-23)

- A no-fill frame renders white: real, but only in dark theme. The inline screen iframe inherited the editor's dark colour-scheme, so Chrome painted an opaque base under it. Fix: inline screens set `colorScheme: "light"` (DesignCanvas.tsx, live iframe style). Figma: a new frame is FFFFFF, and removing the fill leaves it transparent. Design is now transparent in dark mode, and a white screen stays white. Shots: /tmp/parity-paint-transparent-dark-app.png, /tmp/parity-paint-whitescreen-dark-app.png vs /tmp/parity-paint-nofill-frame-figma.png.
- Shift+X leaves the inspector stale: real on the board only. The projection had no authored fill, so `hasBaseFill` was false and the Fill row stayed hidden. Fix: `hasBaseFill` also counts `vectorFillGradient` (fill-properties.tsx). Shots: /tmp/parity-paint-boardswap-{before,after}-app.png vs /tmp/parity-paint-openswap{-before,}-figma.png. Remaining difference: Design shows the open-path fill with eye-off, because the pen agent writes fill-opacity="0"; Figma shows Fill 000000 with the eye on.
- Doubled "Solid" label: real. Radix autofocused the first tab, so the header label and the focused tab both read "Solid". Fix: the picker PopoverContent focuses the dialog itself (DesignColorPicker.tsx onOpenAutoFocus). Shots: /tmp/parity-paint-solidlabel-app.png vs /tmp/parity-paint-solidlabel-figma.png.
- Corner clamp now matches Figma's rule (measured: a 100×40 path at r=1000 becomes a stadium; a rhombus becomes its incircle). Each segment is shared by its two corners in proportion to the length their radius needs, and an open end cedes the whole segment. The JEV holes are now circles like Figma's. Remaining difference: the logo's outer corners are built from 0.005–0.2-unit micro-segments and smooth curve joins, which Figma still rounds visibly at r40/r100 while Design keeps them nearly sharp. Side-by-side: /tmp/parity-pen-r3-side-by-side.png.
- Round 2 (svgpaste): Figma Fill→Crop (measured via UI on a 1999×1168 image at 396×139) keeps the Fill region (imageTransform [[1,0,0],[0,0.6007,0.1996]]) — no stretch; a later non-uniform resize stretches in Figma. Design Crop = object-fit:cover + `--an-image-scale:crop` (never distorts; later resize crops instead of stretching — known difference, no crop-region editing yet). Custom properties were silently dropped by the legacy inline-style commit fallback (`element.style[prop] = v`); fixed with setProperty. Image paste now bases durable writes on durable content, not the live preview — a stale preview (set by an earlier image paste) reverted a later reparent (user's tabler Frame ejected from the board frame).

## Paint agent, round 3 (2026-09-23)

- Gradient fill on an open pen path showed the eye as hidden: real. The open-path "no paint" marker (`fill-opacity="0"`) was the same signal used to hide vector gradient fills. Fix: hide or show a vector gradient by wrapping its stops (`color-mix(... 0%, transparent)`, the same wrapper solid fills use), via `svgPaintGradientWithOpacity` / `svgPaintGradientOpacity` in shared/svg-paint-gradient.ts. `fill-opacity` now means only "open path". Verified: after Shift+X the eye is on; one toggle writes wrapped stops (hidden) and keeps fill-opacity 0; a second toggle restores the original stops. Shots: /tmp/parity-paint-r3-openeye-app.png vs /tmp/parity-paint-openswap-figma.png (both: Fill eye on, Stroke empty, nothing painted on canvas); hidden state /tmp/parity-paint-r3-openeye-hidden-app.png; stop-color color-mix renders transparent: /tmp/parity-paint-r3-stopmix.png.
- Deferred by the user: Inside/Outside for imported vectors, gradient-stroke stops and handles, extra stroke paint types, colour-styles list.
- ROUND 3 alt labels over the size badge: NOT a difference. Figma measured with the same layout (160x100 at 200,200 vs 520,380) at 31% and 50%: its "80" label also sits on top of the "160 × 100" badge (/tmp/parity-overlays-altlabel-{31,50}-figma.png vs /tmp/lead-alt-design.png). No change.
- ROUND 3 Scale section: factor field now carries Figma's square-arrow icon instead of a "×" label, and the K tool icon (toolbar + shortcuts panel) is IconResize to match Figma's scale glyph (/tmp/parity-overlays-kscale-section-app.png vs templates/design/.tmp/parity/overlays/fig-scale-1-k.png).
- DEFERRED by user: constant-size selection chrome at high zoom. Measured at 750% (overview max via Shift+2): iframe scale 6.667, --agent-native-editor-chrome-line-scale 0.15 (correct), but the selection outline is a literal 1px border so it paints 6.67 screen px; resize-edge hit spans 9.9px. The counter-scale var exists but the outline/handles don't use it. No code changed.
- In-edit Vector section: with a point selected, the inspector swaps Position/Layout for a "Vector" section (new string in all 11 locales) showing the point's X/Y (editable, saved) and the mirroring toggles, as Figma does. Still different: Figma's section also holds the alignment row and the corner radius, and shows even with no point selected. Compare: /tmp/parity-pen-vector-section-compare.png.
- Deferred at the user's request: outer-corner clustering for imported outlines (a trial implementation was removed, not shipped); vector edit on pasted/multi-subpath SVGs.

### In-canvas text editing — round 2 clip (44Q56RjmuBBz, 6:56–8:43)

- Range recolour: every preview tick of a picker drag became a source write, so about 40 writes queued (the saved file lagged about 4 s behind with stale colours) and echoed back over the live range. Fix: range style-change messages carry `phase`; preview ticks restyle the range without writing, and only the commit writes. After: saved = final colour immediately, and the range stays on "something".
- Dark block over the range: the suspended range was re-added as the frame's native selection, which Chrome paints grey/opaque in an unfocused frame. Fix: keep the range in `suspendedTextEditRange` only (removed from the frame's native selection after a recolour and after a morph) = Figma hides the highlight but keeps the range.
- Picker lag / flicker root: the board canvas got `tweakValues={{}}` (fresh object every render) → every MultiScreenCanvas render replayed the whole editor state (select-elements…) → the bridge re-posted board-selection-rect → setState → render: 279 replays in ~7 s, now 3. Also, while text editing, the caret overlay rewrote `data-agent-native-drawn-caret` every frame → MutationObserver → selection chrome re-posted 60/s, now 0.
- White default text: dark editor → board render copy uses color-scheme:dark → the default text colour (CanvasText) turns white, so `currentColor` text in a white frame rendered white. Fix: the render style pins `html{color:#000}`. Dark theme now renders black and shows Fill 000000, matching the Figma default 000000 measured earlier.
- Inspector: Line height flipped 120% → 19.2px on entering edit (a range run had no inline line-height) → now inherited from the text root. W/H now update live while typing (text-editing-state carries the box size). Remaining: W/H go blank after committing a NEWLY created text until it is reselected (projection info replaces the measured one) — deferred.
- Single click on a text inside a top-level board Frame selected the Frame; Figma (/tmp/parity-r2-frametext-after-figma.png) selects the text → a top-level board frame now acts as an artboard scope; nested frames keep click-through.
- "somethin g" highlight gap: not reproduced headless.

### In-canvas text editing — clip 8mm3oAIjrKoi (range recolour goes stale after ~18s)

Repro r3-successive.mjs (board text in a Frame, 8 successive picker clicks/drags on the same range): the step "drag that ends over the canvas" was the trigger. On pointer-enter, the canvas's `focusScrollSurface` (DesignCanvas) took keyboard focus from the picker, the inspector focus flag went false, the bridge dropped the suspended range, and every later pick restyled the text ROOT (hidden under the span's own colour), so the canvas stayed grey while the picker changed — the user's 18–44 s. Fix: no focus theft while the focused element is inside an open popper (plus the existing text-edit guard). After: canvas colour = picker hex at every step (CC8F8F, 7A6262, B31212, BF2626, 808080, E61717, CC3D3D); screenshot /tmp/parity-r3-after-steps-crop-app.png. Figma keeps applying to the range (fig14).
Picker knob: it stays at the release point in 3 of 4 drags; one low-saturation drag re-synced its knob from y 0.40 to 0.52 (the committed hex) — open.

### In-canvas text editing — W/H blank after commit; picker knob away from cursor

- W/H "Hug" with no number after committing a text: the post-commit selection was rebuilt from the bridge payload, which carries no `sourceLayerIdentity`; without it the inspector could not resolve the layer's size. Fix: screen/active text commit keep `{screenId, nodeId}` of the committed node. After: "W 95 Hug | H 19.2 Hug" stays through Escape, for both new and existing text (/tmp/parity-r4-wh-after-commit-app.png). Figma always shows the numbers (/tmp/parity-r2-frametext-after-figma.png).
- Knob away from the release point: two causes. (1) A press on the saturation field / sliders started a host text selection, and the next press inside it became a native drag → pointercancel → the gesture froze partway (knob and colour stuck where it stopped). Fix: preventDefault + explicit focus on pointerdown. (2) The first pick added a swatch to "Document colors", the grid wrapped a new row (+24px), and the popover (collision-shifted) slid up under the cursor. Fix: the palette is frozen while the picker is open. After: 8/8 saturation drags (greys included) end with the knob exactly at the release point, a stable popover, and canvas = hex; 3 hue ("H" slider) drags apply to the range (/tmp/parity-r4-picker-after-8-drags-app.png).

### In-canvas text editing — range highlight: keep the selection, hide the paint

The round-2 "remove the frame's native selection" fix broke `text-range-visibility-parity.spec.ts:306` (getSelection() empty after focus moved to the inspector). Now the native selection is kept (and re-added on morph restore as before), and while the inspector holds focus for a text range the bridge sets `data-agent-native-inspector-styling-range` on <html>, where `::selection{background:transparent}` hides the grey block. Verified: spec 306 passes; recolour keeps getSelection()="something", with a transparent ::selection background and no dark box (/tmp/parity-r6-range-hidden-highlight-app.png); 8 picker drags + 3 hue drags still hit the range.
