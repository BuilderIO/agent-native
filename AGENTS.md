# Agent Guidelines

## Image Output

Never save screenshots, images, or other binary artifacts to the repository root or directly inside package directories. If you need to capture screenshots (e.g. for design iteration), save them to a temporary directory outside the repo or use an ephemeral path that won't be committed.
