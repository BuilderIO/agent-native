---
title: "Deployment"
description: "Deploy an Agent-Native app or workspace to a Nitro-compatible platform with a persistent SQL database."
search: "deployment deploy app workspace hosting database Nitro Vercel Netlify Cloudflare Node.js"
---

# Deployment

Agent-Native apps run on [Nitro](https://nitro.build), so the same app can
deploy to Node.js, serverless functions, edge workers, or a managed host. Pick
the path that matches what you are shipping:

<Cards columns={2}>

### :server: [Deploy an app](/docs/deploy-an-app)

Build and launch one standalone app, or deploy one app from a workspace on its
own domain.

### :network: [Deploy a workspace](/docs/workspace-deployment)

Build several apps behind one origin with shared login and same-origin app to
app communication.

</Cards>

## Before you deploy: choose a persistent database {#persistent-database}

Every deployed app needs a persistent SQL database. Local development can use
the SQLite file at `data/app.db`, but container, preview, and serverless
filesystems can be reset at any time.

<Steps>

### Choose a provider

Use any Drizzle-compatible SQL backend, such as Postgres, Neon, Supabase,
Railway, RDS, Turso, or libSQL. The framework detects the database dialect from
the connection URL.

### Set the connection string

Set `DATABASE_URL` in the deployment environment before the first production
deploy. Use `DATABASE_AUTH_TOKEN` too when the provider requires a separate
token, such as Turso or libSQL.

### Add production secrets

Set a stable `BETTER_AUTH_SECRET` for a standalone app. A workspace also needs
`A2A_SECRET` when it uses signed cross-app requests or background handoffs. The
[deployment environment reference](/docs/deployment-environment-variables)
covers the rest of the production configuration.

</Steps>

<Callout tone="info">

In a workspace, every app inherits the root `DATABASE_URL` by default. Set
`<APP_NAME>_DATABASE_URL` only when one app needs a different database.

</Callout>

## Supported deployment targets

Set a Nitro preset for the target you choose. The app code and database
contract stay the same.

<Diagram id="doc-block-deployment-targets" title="Supported deployment targets" summary="Agent-Native apps can deploy to Node.js, Vercel, Netlify, Cloudflare, AWS Lambda, and Deno Deploy. Provider logos are shown for the hosted targets with first-party deployment pages.">

```html
<div class="deployment-target-grid">
  <a class="deployment-target" href="/docs/node-docker">
    <span class="deployment-target-mark text-mark">N</span>
    <strong>Node.js</strong>
  </a>
  <a class="deployment-target" href="/docs/vercel">
    <span class="deployment-target-mark">
      <img src="/integration-logos/vercel.svg" alt="" />
    </span>
    <strong>Vercel</strong>
  </a>
  <a class="deployment-target" href="/docs/netlify">
    <span class="deployment-target-mark">
      <img src="/integration-logos/netlify.svg" alt="" />
    </span>
    <strong>Netlify</strong>
  </a>
  <a class="deployment-target" href="/docs/cloudflare">
    <span class="deployment-target-mark">
      <img src="/integration-logos/cloudflare.svg" alt="" />
    </span>
    <strong>Cloudflare</strong>
  </a>
  <a class="deployment-target" href="/docs/other-platforms#aws-lambda">
    <span class="deployment-target-mark text-mark">AWS</span>
    <strong>AWS Lambda</strong>
  </a>
  <a class="deployment-target" href="/docs/other-platforms#deno-deploy">
    <span class="deployment-target-mark text-mark">D</span>
    <strong>Deno Deploy</strong>
  </a>
</div>
```

```css
.deployment-target-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
}
.deployment-target {
  display: flex;
  align-items: center;
  gap: 10px;
  min-height: 58px;
  padding: 10px 12px;
  border: 1px solid hsl(var(--border));
  border-radius: 10px;
  color: inherit;
  text-decoration: none;
}
.deployment-target:hover {
  border-color: hsl(var(--primary));
}
.deployment-target-mark {
  display: grid;
  width: 30px;
  height: 30px;
  place-items: center;
  flex: 0 0 auto;
}
.deployment-target-mark img {
  width: 26px;
  height: 26px;
  object-fit: contain;
}
.deployment-target-mark.text-mark {
  border-radius: 7px;
  background: hsl(var(--muted));
  font-size: 12px;
  font-weight: 700;
}
@media (max-width: 640px) {
  .deployment-target-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}
```

</Diagram>

## Advanced deployment

Start with the path above. Use these pages when you need provider-specific
settings or production controls:

- [**Node.js & Docker**](/docs/node-docker) - run the default preset directly or in a container
- [**Vercel**](/docs/vercel), [**Netlify**](/docs/netlify), and [**Cloudflare**](/docs/cloudflare) - configure the main hosted targets
- [**Other Platforms**](/docs/other-platforms) - deploy to AWS Lambda or Deno Deploy
- [**SSR Caching**](/docs/ssr-caching) - configure the public shell cache
- [**Deployment Environment Variables**](/docs/deployment-environment-variables) - production secrets, runtime flags, and workspace inheritance
- [**Production Agent Access**](/docs/actions-agent-tools) - control framework tools and code execution
- [**Updating UI in Production**](/docs/updating-ui-in-production) - understand the managed visual-editing workflow

<a id="self-host-quickstart"></a>
<a id="skip-ensure-tables"></a>

For compatibility with older links, the local Docker quickstart now lives on
[Node.js & Docker](/docs/node-docker#self-host-quickstart). Schema setup and
cold-start guidance live in the [deployment environment reference](/docs/deployment-environment-variables#skip-ensure-tables).

## What's next

- [**Deploy an app**](/docs/deploy-an-app)
- [**Deploy a workspace**](/docs/workspace-deployment)
- [**Database**](/docs/server-database#production)
- [**Security**](/docs/security)
- [**Authentication**](/docs/authentication)
