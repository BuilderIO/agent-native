---
title: "Cloudflare"
description: "Implementa una aplicación agent-native en Cloudflare Workers con el ajuste preestablecido cloudflare_module de Nitro."
---

# Cloudflare

## Cloudflare Workers {#cloudflare-workers}

Usa la salida nativa de módulos de Nitro para un Cloudflare Worker. Esto mantiene
SSR habilitado y emite el punto de entrada del Worker más los activos estáticos en
`.output/server` y `.output/public`:

```ts filename="vite.config.ts"
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "cloudflare_module" } })],
});
```

Compila e implementa el Worker generado con Wrangler:

```bash
pnpm build
npx wrangler deploy --config .output/server/wrangler.json
```

También puedes seleccionar el ajuste preestablecido explícitamente con
`NITRO_PRESET=cloudflare_module`. Configura una base de datos PostgreSQL externa
durable antes de implementar. El sistema de archivos de un Worker no es un
almacén persistente de la aplicación.

## Qué sigue

- [**Implementación**](/docs/deployment) — elige una base de datos persistente y un ajuste preestablecido antes de tu primera implementación en producción
- [**Implementación de espacio de trabajo**](/docs/workspace-deployment) — implementa cada aplicación del espacio de trabajo en un solo origen con Netlify o Vercel
- [**Node.js**](/docs/node-js), [**Docker**](/docs/docker), [**Vercel**](/docs/vercel), [**Netlify**](/docs/netlify), [**AWS Lambda**](/docs/aws-lambda) y [**Deno Deploy**](/docs/deno-deploy) — los demás ajustes preestablecidos de Nitro
