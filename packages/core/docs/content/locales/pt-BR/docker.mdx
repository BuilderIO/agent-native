---
title: "Docker"
description: "Localized pointer to the the Docker packaging guide."
---

<!-- i18n-docs-ignore -->

# Docker

Consulte o [guia de Docker em inglês](/docs/docker) e crie o `Dockerfile`
mostrado nele antes de executar `docker build`.

The deployment must use a persistent PostgreSQL database. Configure `DATABASE_URL`
and `BETTER_AUTH_SECRET` in the provider environment. The Nitro preset is
`node`.
