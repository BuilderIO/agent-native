---
title: "Cloudflare"
description: "انشر تطبيق agent-native على Cloudflare Workers باستخدام إعداد Nitro المسبق cloudflare_module."
---

# Cloudflare

## Cloudflare Workers {#cloudflare-workers}

استخدم مخرجات الوحدة الأصلية لـ Nitro من أجل Cloudflare Worker. هذا يبقي
SSR مُفعّلًا ويُصدر نقطة دخول الـ Worker بالإضافة إلى الأصول الثابتة تحت
`.output/server` و`.output/public`:

```ts filename="vite.config.ts"
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "cloudflare_module" } })],
});
```

قم ببناء ونشر الـ Worker المُولَّد باستخدام Wrangler:

```bash
pnpm build
npx wrangler deploy --config .output/server/wrangler.json
```

يمكنك أيضًا تحديد الإعداد المسبق صراحةً باستخدام
`NITRO_PRESET=cloudflare_module`. قم بتكوين قاعدة بيانات PostgreSQL خارجية دائمة
قبل النشر. نظام ملفات الـ Worker ليس مخزنًا دائمًا للتطبيق.

## ما هي الخطوة التالية

- [**النشر**](/docs/deployment) — اختر قاعدة بيانات دائمة وإعدادًا مسبقًا قبل أول عملية نشر إنتاجي
- [**نشر مساحة العمل**](/docs/workspace-deployment) — انشر كل تطبيق في مساحة العمل على أصل واحد باستخدام Netlify أو Vercel
- [**Node.js**](/docs/node-js)، [**Docker**](/docs/docker)، [**Vercel**](/docs/vercel)، [**Netlify**](/docs/netlify)، [**AWS Lambda**](/docs/aws-lambda)، و[**Deno Deploy**](/docs/deno-deploy) — إعدادات Nitro المسبقة الأخرى
