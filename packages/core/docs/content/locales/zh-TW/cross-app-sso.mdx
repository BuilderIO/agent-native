---
title: "跨應用程式 SSO"
description: "區分組織 OIDC 或 SAML 登入與跨應用程式身分聯邦。"
---

# 組織 SSO 與跨應用程式聯邦

組織 SSO 透過公司的 OIDC 或 SAML 身分提供者驗證使用者登入單一 Agent-Native 部署。跨應用程式聯邦則在不同部署間傳遞已驗證的身分或組織內容。兩者的設定與信任邊界不同。

精確註冊的 Agent-Native 應用程式登入頁提供「Continue with Agent-Native」明確
操作。它會啟動 Dispatch 現有的已驗證電子郵件流程。本頁介紹瀏覽器、Desktop
和伺服器端聯邦。

## 跨應用程式聯邦

### Desktop 工作區

工作區登入是 Desktop 設定中的明確操作。啟用後，Desktop 可以透過專用
broker 為符合條件且已註冊的應用程式設定登入。每個目標應用程式仍然擁有
自己的使用者儲存、工作階段和存取檢查。Desktop 不會攔截瀏覽器前往應用
程式登入頁面的匿名導覽。

應用程式必須同時符合以下條件：

- Desktop 為該應用程式啟用工作區 SSO。
- 生產 URL 是精確的 HTTPS 位址。
- Dispatch 為該應用程式保存了精確的伺服器端註冊。
- <code>desktop.workspace-sso</code> 發布旗標已啟用。

裝置層級偏好設定 <code>desktopSsoEnabled</code> 預設啟用。明確設定為
false 仍然表示退出。如果旗標、偏好設定、註冊或資格檢查不可用，普通的
應用程式登入仍可使用。

### 伺服器端聯邦

broker 和伺服器端呼叫方使用短時、由伺服器驗證的協定。精確註冊的第一方
應用程式登入頁也可在使用者選擇「Continue with Agent-Native」後啟動相同協定：

1. 呼叫方要求一個精確的已註冊應用程式繫結。
2. Dispatch 驗證工作區身分並返回短時的一次性授權碼。
3. 目標應用程式伺服器使用 PKCE 驗證器兌換程式碼。
4. 目標應用程式驗證簽署斷言、精確應用程式繫結、回呼、範圍、nonce 和過期時間。
5. 目標應用程式按已驗證電子郵件連結或建立本機使用者，並簽發自己的普通工作階段。

斷言只透過伺服器對伺服器交換返回。它不會出現在瀏覽器 URL 中，也不是
可重複使用的 bearer token。

## 組織聯邦

<code>organization.cross-app-federation</code> 旗標預設關閉。啟用後，來源
應用程式向 Dispatch 註冊本機組織，並儲存精確的
<code>(identity_authority, identity_id)</code> 對應。目標應用程式只接受
來自伺服器驗證斷言的規範組織 ID、名稱和已驗證成員角色。

組織名稱、電子郵件網域和瀏覽器 Cookie 不是連結鍵。沒有對應的本機組織在
明確遷移前保持不變。已連結的成員關係仍使用目標應用程式的普通存取檢查。

組織聯邦為每個已註冊來源應用程式使用獨立認證：

- 在來源應用程式設定 <code>AGENT_NATIVE_IDENTITY_FEDERATION_SECRET</code>。
- 在 Dispatch 設定相符的
  <code>AGENT_NATIVE_IDENTITY_FEDERATION_SECRET_&lt;NORMALIZED_APP_ID&gt;</code>
  。
- 不要為此權限邊界重複使用 <code>A2A_SECRET</code>。

應用程式 ID 後綴會將每段非英數字元替換為底線，並將結果轉換為大寫。
例如，應用程式 ID <code>workspace-reports</code> 使用

<code>AGENT_NATIVE_IDENTITY_FEDERATION_SECRET_WORKSPACE_REPORTS</code>。

身分和組織寫入只會新增資料。相同電子郵件的現有使用者會原樣重用。新的
已驗證電子郵件會建立本機使用者。停用聯邦不會刪除資料。

## 設定

<code>AGENT_NATIVE_IDENTITY_HUB_URL</code> 用於在自我託管應用程式需要明確設定
時，指定伺服器端和瀏覽器路徑使用的 Dispatch。精確註冊的第一方應用程式登入頁
會提供「Continue with Agent-Native」明確操作。<code>browser.identity-sso</code>
發布旗標仍控制靜默 <code>prompt=none</code> 登入和本機登入後的初始化。普通
本機登入仍可作為備援。

對於自我託管的 Dispatch，請透過

<code>IDENTITY_SSO_APP_REGISTRY_JSON</code> 為每個自訂應用程式註冊精確的
應用程式 ID、用戶端 ID、HTTPS 來源、回呼路徑和 <code>identity-sso</code> 能力。

生產註冊必須使用 HTTPS 和精確的回呼路徑。Localhost 只允許用於本機開發。
未知子網域、不相符的應用程式 ID、任意路徑和跨來源回呼都會被拒絕。

## 驗證和回滾

部署後：

1. 驗證瀏覽器中的電子郵件、密碼、魔法連結和已設定的提供者登入仍然有效。
2. 驗證頁面提供「Continue with Agent-Native」操作，同時本機登入仍可使用。
   關閉 <code>browser.identity-sso</code> 時，確認這兩種登入方式仍可使用。
   啟用後，確認靜默 <code>prompt=none</code> 登入和本機表單備援正常。
3. 如果啟用了 Desktop SSO，從 Desktop 設定中的明確操作驗證一個精確註冊的應用程式。
4. 如果啟用了組織聯邦，驗證目標應用程式中的精確組織 ID 和成員角色。

要回滾靜默瀏覽器登入或 Desktop 路徑，請關閉相應功能旗標。要從自我託管應用程式
移除瀏覽器明確操作，請移除 <code>AGENT_NATIVE_IDENTITY_HUB_URL</code> 或其精確
應用程式註冊，然後重新部署。本機登入仍可使用，已新增的身分和組織記錄也會保留。

## 相關文件

- [驗證](/docs/authentication)
- [A2A 協定](/docs/a2a-protocol)
- [Dispatch](/docs/dispatch)
- [安全性與資料範圍](/docs/security)
- [多應用程式工作區](/docs/multi-app-workspace)

透過 OIDC 或 SAML 進行企業登入，與應用程式之間的身分聯合不同。[管理式部署指南](/docs/administered-deployments)介紹企業部署模式。啟用 `browser.identity-sso` 後，應用程式會嘗試靜默的 `prompt=none` 檢查，並保留本機登入表單作為備援。請參閱[跨應用程式連線設計](/docs/cross-app-connect-design)。

## 瀏覽器身分 SSO 推出步驟

先只為一個測試電子郵件啟用 `browser.identity-sso`。登入 Dispatch、登出目標應用程式，再重新載入以確認靜默檢查。沒有 hub 工作階段時再次測試，確認 `login_required` 回到本機登入表單後再擴大範圍。首次啟用可能需要一次性撤銷現有工作階段。
