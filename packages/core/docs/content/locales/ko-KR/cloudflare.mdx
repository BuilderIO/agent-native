---
title: "Cloudflare"
description: "cloudflare_module Nitro 프리셋으로 agent-native 앱을 Cloudflare Workers에 배포하세요."
---

# Cloudflare

Cloudflare Worker에는 Nitro의 네이티브 모듈 출력을 사용하세요. 이는 SSR을
계속 활성화한 상태로 유지하며 Worker 진입점과 정적 자산을
`.output/server` 및 `.output/public`에 생성합니다:

```ts filename="vite.config.ts"
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "cloudflare_module" } })],
});
```

Build and deploy the generated Worker with Wrangler:

```bash
pnpm build
npx wrangler deploy --config .output/server/wrangler.json
```

`NITRO_PRESET=cloudflare_module`로 프리셋을 명시적으로 선택할 수도 있습니다.
배포 전에 내구성 있는 외부 PostgreSQL 데이터베이스를 구성하세요. Worker 파일 시스템은
영구적인 애플리케이션 저장소가 아닙니다.

## 다음 단계

- [**Deployment**](/docs/deployment) — 첫 프로덕션 배포 전에 영구 데이터베이스와 프리셋을 선택하세요
- [**Workspace Deployment**](/docs/workspace-deployment) — Netlify 또는 Vercel로 워크스페이스의 모든 앱을 하나의 오리진에 배포하세요
- [**Node.js**](/docs/node-js), [**Docker**](/docs/docker), [**Vercel**](/docs/vercel), [**Netlify**](/docs/netlify), [**AWS Lambda**](/docs/aws-lambda), [**Deno Deploy**](/docs/deno-deploy) — 그 외 Nitro 프리셋
