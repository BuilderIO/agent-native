---
title: "Cloudflare"
description: "使用 cloudflare_module Nitro 预设将 agent-native 应用部署到 Cloudflare Workers。"
---

# Cloudflare

使用 Nitro 原生的模块输出来生成一个 Cloudflare Worker。这样可以
保持 SSR 启用，并在 `.output/server` 和 `.output/public` 下生成
Worker 入口点和静态资源:

```ts filename="vite.config.ts"
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "cloudflare_module" } })],
});
```

Build and deploy the generated Worker with Wrangler:

```bash
pnpm build
npx wrangler deploy --config .output/server/wrangler.json
```

你也可以用 `NITRO_PRESET=cloudflare_module` 显式选择这个预设。
部署之前请配置一个持久的外部 PostgreSQL 数据库。Worker 的文件系统
不是一个持久的应用存储。

## 下一步

- [**Deployment**](/docs/deployment) — 在第一次生产部署之前选择一个持久数据库和一个预设
- [**Workspace Deployment**](/docs/workspace-deployment) — 使用 Netlify 或 Vercel 将工作区中的每个应用部署到同一来源
- [**Node.js**](/docs/node-js)、[**Docker**](/docs/docker)、[**Vercel**](/docs/vercel)、[**Netlify**](/docs/netlify)、[**AWS Lambda**](/docs/aws-lambda) 和 [**Deno Deploy**](/docs/deno-deploy) — 其他 Nitro 预设
