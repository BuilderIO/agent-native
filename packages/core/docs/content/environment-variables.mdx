---
title: "Environment Variables"
description: "Reference for Agent-Native framework, workspace, authentication, and deployment environment variables."
search: "environment variables env .env DATABASE_URL APP_URL BETTER_AUTH_SECRET Google OAuth workspace app"
---

# Environment Variables

This page covers the environment variables owned by the Agent-Native framework
and workspace runtime. It is not a setup inventory of every template or
deployment system in this repository. Template-only values stay in the
maintainer inventory; the exact index below covers keys hard-coded in shared
first-party runtime and app code.

For a normal app, start with [Deployment](/docs/deployment#environment-variables)
and [Authentication](/docs/authentication). Keep user-, organization-, and
workspace-scoped credentials in the database-backed secret or connection store;
use environment variables for deploy-level configuration.

For committed, non-secret app defaults such as first-run onboarding, use the
[Agent-Native app configuration guide](/docs/agent-native-config) instead of
adding another browser-exposed environment setting. Keep credentials and
deployment-specific values in the environment or scoped secret store.

## Common application and deployment variables

| Variable                                    | What it controls                                                                                | Supported values or format                                                                                     |
| ------------------------------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `DATABASE_URL`                              | Primary SQL connection. Local development falls back to SQLite when it is unset.                | A provider-specific SQL URL, or unset for local SQLite.                                                        |
| `DATABASE_AUTH_TOKEN`                       | Separate database token when the database provider requires one.                                | An opaque provider token, or unset when the URL includes authentication.                                       |
| `APP_URL`                                   | Canonical public origin used for auth, OAuth, A2A, webhooks, and generated links.               | An absolute public `http(s)` origin.                                                                           |
| `BETTER_AUTH_URL`                           | Public origin used by Better Auth for cookies and callbacks.                                    | An absolute public `http(s)` origin.                                                                           |
| `APP_BASE_PATH`                             | Mounts an app under a path such as `/mail`.                                                     | Unset for a standalone app, or a slash-prefixed path.                                                          |
| `PORT`                                      | Local Node/Nitro server port.                                                                   | A decimal TCP port.                                                                                            |
| `NITRO_PRESET`                              | Build and deployment adapter.                                                                   | A preset supported by the target host, such as `node`, `netlify`, `vercel`, or `cloudflare_pages`.             |
| `BETTER_AUTH_SECRET`                        | Better Auth session signing and the legacy encryption fallback. Required in production.         | A fresh random value of at least 32 characters.                                                                |
| `OAUTH_STATE_SECRET`                        | Dedicated signing key for OAuth state envelopes. Falls back to `BETTER_AUTH_SECRET` when unset. | A fresh random value of at least 32 characters.                                                                |
| `A2A_SECRET`                                | Shared HMAC for trusted cross-app A2A calls and signed handoffs.                                | One shared random value across trusted apps.                                                                   |
| `SECRETS_ENCRYPTION_KEY`                    | Legacy app-local/shared encryption fallback.                                                    | A stable encryption key; prefer the workspace key for new shared vaults.                                       |
| `WORKSPACE_SECRETS_ENCRYPTION_KEY`          | Stable encryption key for a workspace-shared secrets vault.                                     | The same stable key in every app that reads the vault.                                                         |
| `WORKSPACE_SECRETS_ENCRYPTION_KEY_PREVIOUS` | Previous workspace key during rotation.                                                         | The old key only; remove it after migration.                                                                   |
| `ANTHROPIC_API_KEY`                         | Optional deploy-level fallback for the embedded production agent.                               | An API key; prefer scoped credentials for multi-tenant apps.                                                   |
| `AUTH_MODE`                                 | Selects the local authentication behavior used by CLI and agent flows.                          | Unset or `local`; this is not a browser login bypass.                                                          |
| `AUTH_DISABLED`                             | Disables auth for local or preview-only work.                                                   | `true` or `1`; never use on a public production app.                                                           |
| `AUTH_MAGIC_LINK`                           | Controls the password-first versus magic-link sign-in path.                                     | `0` keeps password-first sign-in; otherwise a configured email transport can enable magic links.               |
| `AUTH_SKIP_EMAIL_VERIFICATION`              | Skips signup email verification for local QA or previews.                                       | `1`; never use it to weaken production auth.                                                                   |
| `ACCESS_TOKEN` / `ACCESS_TOKENS`            | Static bearer fallback for MCP and Connect clients.                                             | One token or a delimited token list; these are not browser auth.                                               |
| `AGENT_PROD_CODE_EXECUTION`                 | Production code-execution policy.                                                               | `off`, `sandboxed`, or `trusted`. See [Production Code Execution](/docs/deployment#production-code-execution). |
| `AGENT_NATIVE_SSR_CACHE`                    | Deployment-wide public SSR shell cache policy.                                                  | Unset/`on`, `off`, or a duration such as `30s` or `5m`. See [SSR Caching](/docs/deployment#ssr-caching).       |
| `COOKIE_DOMAIN`                             | Shares auth cookies across same-database subdomains when explicitly configured.                 | A cookie domain.                                                                                               |
| `CORS_ALLOWED_ORIGINS`                      | Cross-origin request allowlist.                                                                 | A comma-separated list of exact origins.                                                                       |
| `WEBHOOK_BASE_URL`                          | Optional public base URL for self-callback and webhook targets.                                 | An absolute public `http(s)` origin.                                                                           |
| `APP_NAME`                                  | App identity used by workspace and app-scoped configuration.                                    | A short app name, such as `mail`.                                                                              |
| `APP_ID` / `APP_TEMPLATE`                   | Optional app identity and generated-template metadata.                                          | App-owned strings.                                                                                             |
| `NODE_ENV`                                  | Node runtime mode.                                                                              | Normally `development`, `test`, or `production`.                                                               |
| `CI`                                        | Marks CI-driven test and build behavior.                                                        | A CI-provided truthy value.                                                                                    |
| `DEBUG`                                     | Enables resolver-specific debug logging.                                                        | A resolver-supported truthy or namespace value.                                                                |

Generate strong secrets with:

```bash
openssl rand -hex 32
```

## Google sign-in

Set one complete Google OAuth client pair to show the Google sign-in flow.
Prefer a low-scope identity-only client for app login, and keep API access in a
separate connection when a product needs broader Google scopes.

| Variable                       | What it controls                                                                                | Supported values or format                |
| ------------------------------ | ----------------------------------------------------------------------------------------------- | ----------------------------------------- |
| `GOOGLE_SIGN_IN_CLIENT_ID`     | Preferred low-scope Google OAuth client ID for app login.                                       | The client ID from Google Cloud Console.  |
| `GOOGLE_SIGN_IN_CLIENT_SECRET` | Secret for the preferred sign-in client.                                                        | The matching client secret.               |
| `GOOGLE_CLIENT_ID`             | Backwards-compatible sign-in fallback and provider OAuth client ID for Google API integrations. | The matching Google OAuth client ID.      |
| `GOOGLE_CLIENT_SECRET`         | Secret for `GOOGLE_CLIENT_ID`.                                                                  | The matching client secret.               |
| `GOOGLE_AUTH_MODE`             | Chooses the sign-in interaction.                                                                | `auto` (default), `popup`, or `redirect`. |

Register the callback URL in Google Cloud Console as the app's public URL,
including its base path, followed by
`/_agent-native/google/callback`. See [Authentication - Google OAuth](/docs/authentication#social-providers).

`GOOGLE_LEGACY_CLIENT_ID` and `GOOGLE_LEGACY_CLIENT_SECRET` are accepted for
older Google provider connections. `GOOGLE_SERVICE_ACCOUNT_KEY` and
`GOOGLE_APPLICATION_CREDENTIALS` are server-side service-account options for
specific Google API integrations; they are not required for browser sign-in.

## Optional AI, search, and email fallbacks

These are deploy-level fallbacks for single-tenant or local use. Hosted,
multi-user apps should prefer the scoped credential and connection store.

| Variable                       | What it controls                                         | Supported values or format                              |
| ------------------------------ | -------------------------------------------------------- | ------------------------------------------------------- |
| `OPENAI_API_KEY`               | OpenAI model, realtime voice, or transcription fallback. | An OpenAI API key.                                      |
| `OPENAI_BASE_URL`              | OpenAI-compatible endpoint used with `OPENAI_API_KEY`.   | An absolute `http(s)` URL without embedded credentials. |
| `GOOGLE_GENERATIVE_AI_API_KEY` | Gemini model fallback for the built-in AI engine.        | A Google AI API key.                                    |
| `GEMINI_API_KEY`               | Gemini transcription fallback.                           | A Google AI API key.                                    |
| `OPENROUTER_API_KEY`           | OpenRouter model fallback.                               | An OpenRouter API key.                                  |
| `GROQ_API_KEY`                 | Groq model or transcription fallback.                    | A Groq API key.                                         |
| `MISTRAL_API_KEY`              | Mistral model fallback.                                  | A Mistral API key.                                      |
| `COHERE_API_KEY`               | Cohere model fallback.                                   | A Cohere API key.                                       |
| `BRAVE_SEARCH_API_KEY`         | Brave web-search fallback.                               | A Brave Search API key.                                 |
| `TAVILY_API_KEY`               | Tavily web-search fallback.                              | A Tavily API key.                                       |
| `EXA_API_KEY`                  | Exa web-search fallback.                                 | An Exa API key.                                         |
| `FIRECRAWL_API_KEY`            | Firecrawl web-search fallback.                           | A Firecrawl API key.                                    |
| `RESEND_API_KEY`               | Resend transactional email delivery.                     | A Resend API key.                                       |
| `SENDGRID_API_KEY`             | SendGrid transactional email delivery.                   | A SendGrid API key.                                     |
| `EMAIL_FROM`                   | Sender address used for transactional email.             | A valid email address or display-name address.          |
| `EMAIL_AGENT_ADDRESS`          | Inbound address used by email-enabled apps.              | A valid email address.                                  |
| `EMAIL_INBOUND_WEBHOOK_SECRET` | Verifies inbound email webhook signatures.               | A stable random secret.                                 |

## Exact hard-coded keys

The framework source also contains advanced, optional, integration, desktop,
and host-detection keys. This collapsed index is the completeness check for
those names. Every entry below is an exact key found in first-party runtime or
app source - there are no wildcard families here. Most are not required for a
normal app, and a host-detection key does not mean you should set it yourself.

<details>
<summary>Show the complete exact key index</summary>

### Framework and workspace runtime

`A2A_ALLOW_UNSIGNED_INTERNAL`, `A2A_PROCESSING_LIFETIME_MAX_MS`, `A2A_QUEUED_LIFETIME_MAX_MS`, `A2A_SECRET`, `AGENT_APP`, `AGENT_CHAT_DURABLE_BACKGROUND`, `AGENT_CHAT_FORCE_BACKGROUND_RUNTIME`, `AGENT_CHAT_FOREGROUND_SELF_CHAIN`, `AGENT_CLI_COMMAND`, `AGENT_ENGINE`, `AGENT_ENGINE_PREFER_BYO_KEY`, `AGENT_ERRORED_RUN_RETENTION_MS`, `AGENT_INTEGRATION_DURABLE_DISPATCH`, `AGENT_INTEGRATION_DURABLE_DISPATCH_SCOPES`, `AGENT_MAX_ITERATIONS`, `AGENT_MAX_OUTPUT_TOKENS`, `AGENT_MAX_RUN_INPUT_TOKENS`, `AGENT_MODE`, `AGENT_MODEL`, `AGENT_NATIVE_A2A_ALLOWED_ORIGINS`, `AGENT_NATIVE_ALLOW_LOCAL_FILES_IN_PRODUCTION`, `AGENT_NATIVE_ALLOW_UNVERIFIED_WEBHOOKS`, `AGENT_NATIVE_ANALYTICS_ALLOW_LOCALHOST`, `AGENT_NATIVE_ANALYTICS_ENDPOINT`, `AGENT_NATIVE_ANALYTICS_FLUSH_MODE`, `AGENT_NATIVE_ANALYTICS_ORG_NAME`, `AGENT_NATIVE_ANALYTICS_PUBLIC_KEY`, `AGENT_NATIVE_APP`, `AGENT_NATIVE_APP_ID`, `AGENT_NATIVE_APPLE_TEAM_ID`, `AGENT_NATIVE_AUDIT_ENABLED`, `AGENT_NATIVE_AUDIT_RETENTION_DAYS`, `AGENT_NATIVE_BRIDGE_TOKEN`, `AGENT_NATIVE_BROWSER_EXTENSION_IDS`, `AGENT_NATIVE_BUILD_DEPLOY_CONTEXT`, `AGENT_NATIVE_BUILD_GA_MEASUREMENT_ID`, `AGENT_NATIVE_BUILD_GTM_CONTAINER_ID`, `AGENT_NATIVE_BUILD_ID`, `AGENT_NATIVE_BUILD_SHA`, `AGENT_NATIVE_BUILDER_GATEWAY_TIMEOUT_MS`, `AGENT_NATIVE_BUILDER_RELAY_SECRET`, `AGENT_NATIVE_BUILDER_RELAY_TARGET_DOMAIN_SUFFIXES`, `AGENT_NATIVE_BUILDER_RELAY_TARGET_ORIGINS`, `AGENT_NATIVE_BUILDER_WAITLIST_FORM_ID`, `AGENT_NATIVE_BUILDER_WAITLIST_FORMS_ORIGIN`, `AGENT_NATIVE_CLIENT_COMPATIBILITY_VERSION`, `AGENT_NATIVE_CODE_AGENT_FAKE_RESPONSE`, `AGENT_NATIVE_CODE_AGENTS_HOME`, `AGENT_NATIVE_CODE_TOOL_PROFILE`, `AGENT_NATIVE_CODE_USAGE_FILE`, `AGENT_NATIVE_COMPUTER_BRIDGE_TOKEN`, `AGENT_NATIVE_COMPUTER_BRIDGE_URL`, `AGENT_NATIVE_COMPUTER_CAPABILITIES`, `AGENT_NATIVE_CREATE_USE_LOCAL_CORE`, `AGENT_NATIVE_DEBUG_AGENT_ENGINE_DETECT`, `AGENT_NATIVE_DEBUG_CREDENTIAL_RESOLVE`, `AGENT_NATIVE_DEBUG_ERRORS`, `AGENT_NATIVE_DEP_SOURCEMAPS`, `AGENT_NATIVE_DESIGN_PREVIEW_DEBUG`, `AGENT_NATIVE_DESKTOP_CHILD`, `AGENT_NATIVE_DESKTOP_COMPUTER_MCP_TOKEN`, `AGENT_NATIVE_DESKTOP_COMPUTER_MCP_URL`, `AGENT_NATIVE_DESKTOP_SHORTCUT_DEBUG`, `AGENT_NATIVE_DESKTOP_UPDATE_FEED_URL`, `AGENT_NATIVE_DISABLE_AUTO_DEV_ACCOUNT`, `AGENT_NATIVE_DISABLE_BUILDER_WAITLIST_FORM`, `AGENT_NATIVE_DISABLE_MCP_AUTODETECT`, `AGENT_NATIVE_EXPERIMENT_ADMIN_EMAILS`, `AGENT_NATIVE_FEATURE_FLAG_ADMIN_EMAILS`, `AGENT_NATIVE_FRAMEWORK_ROOT`, `AGENT_NATIVE_HEALTH_STRICT_SCHEMA`, `AGENT_NATIVE_HTTP_TELEMETRY_SAMPLE_RATE`, `AGENT_NATIVE_IDENTITY_HUB_URL`, `AGENT_NATIVE_INTEGRATION_A2A_TIMEOUT_MS`, `AGENT_NATIVE_KEYS_WORKSPACE_FALLBACK`, `AGENT_NATIVE_LOCAL_BUILDER_ENV`, `AGENT_NATIVE_MANIFEST`, `AGENT_NATIVE_MANIFEST_PATH`, `AGENT_NATIVE_MAX_SUBAGENT_DEPTH`, `AGENT_NATIVE_MCP_APPS_INLINE`, `AGENT_NATIVE_MCP_APPS_INLINE_ALLOW_EMAILS`, `AGENT_NATIVE_MCP_CLIENT_CONNECT_TIMEOUT_MS`, `AGENT_NATIVE_MCP_CONFIG_REFRESH_MS`, `AGENT_NATIVE_MCP_DEV_OPEN`, `AGENT_NATIVE_MCP_FULL_CATALOG`, `AGENT_NATIVE_MCP_HUB_MULTI_ORG`, `AGENT_NATIVE_MCP_HUB_TOKEN`, `AGENT_NATIVE_MCP_HUB_URL`, `AGENT_NATIVE_MOBILE_DISABLE_REMOTE_PUSH`, `AGENT_NATIVE_NO_OPEN`, `AGENT_NATIVE_NO_PROMPT`, `AGENT_NATIVE_OM_OBSERVATION_TOKEN_THRESHOLD`, `AGENT_NATIVE_OM_RECENT_RAW_MESSAGE_COUNT`, `AGENT_NATIVE_OM_REFLECTION_TOKEN_THRESHOLD`, `AGENT_NATIVE_ORG_NAME`, `AGENT_NATIVE_OWNER_EMAIL`, `AGENT_NATIVE_PACKAGED_MULTI_FRONTIER_SMOKE`, `AGENT_NATIVE_PREVIEW_TOKEN`, `AGENT_NATIVE_PRIVATE_BLOB_PUBLIC_UPLOAD_FALLBACK`, `AGENT_NATIVE_PROJECT_ROOT`, `AGENT_NATIVE_PROVIDER_API_PERSIST_COOLDOWNS`, `AGENT_NATIVE_PROVIDER_API_QUOTA_GOVERNOR`, `AGENT_NATIVE_REALTIME_GATEWAY_URL`, `AGENT_NATIVE_REALTIME_HMAC_SECRET`, `AGENT_NATIVE_REALTIME_TRANSPORT`, `AGENT_NATIVE_RELEASE`, `AGENT_NATIVE_REMOTE_DEVICE_PATH`, `AGENT_NATIVE_RESVG_PACKAGE`, `AGENT_NATIVE_ROUTE_READY_TIMEOUT_MS`, `AGENT_NATIVE_SANDBOX`, `AGENT_NATIVE_SERVERLESS_FFMPEG_ARCH`, `AGENT_NATIVE_SKILLS_DIRECT`, `AGENT_NATIVE_SMOOTH_STREAM`, `AGENT_NATIVE_SSR_CACHE`, `AGENT_NATIVE_SYNC_EVENTS_DISABLE`, `AGENT_NATIVE_SYNC_EVENTS_ENABLE_IN_TESTS`, `AGENT_NATIVE_TELEMETRY_DISABLED`, `AGENT_NATIVE_TEMPLATE`, `AGENT_NATIVE_TEMPLATE_GATEWAY_URL`, `AGENT_NATIVE_THREAD_DEBUG_DATABASES`, `AGENT_NATIVE_TOKEN`, `AGENT_NATIVE_TRACE_RETENTION_DAYS`, `AGENT_NATIVE_URL`, `AGENT_NATIVE_USE_TEMPLATE_GATEWAY`, `AGENT_NATIVE_WORKSPACE`, `AGENT_NATIVE_WORKSPACE_APP_ID`, `AGENT_NATIVE_WORKSPACE_APPS_JSON`, `AGENT_ORG_ID`, `AGENT_PROD_CODE_EXECUTION`, `AGENT_PROGRESS_RUN_STALE_MS`, `AGENT_PROMPT_CACHE_TTL`, `AGENT_RUN_RETENTION_MS`, `AGENT_RUN_SOFT_TIMEOUT_MS`, `AGENT_TERMINAL_DEBUG`, `AGENT_TERMINAL_ENABLED`, `AGENT_TERMINAL_PORT`, `AGENT_USER_EMAIL`, `AGENT_USER_NAME`, `AGENT_USER_TIMEZONE`, `MCP_CLIENT_CONNECT_TIMEOUT_MS`, `MCP_DEBUG_INIT`, `MCP_OAUTH_ACCESS_TOKEN_TTL`, `VITE_AGENT_NATIVE_ANALYTICS_PUBLIC_KEY`, `VITE_AGENT_NATIVE_APP`, `VITE_AGENT_NATIVE_FIRST_RUN_ONBOARDING`, `VITE_AGENT_NATIVE_FIRST_RUN_ONBOARDING_SKIP_INTEGRATIONS`, `VITE_AGENT_NATIVE_TEMPLATE`, `VITE_AGENT_NATIVE_TEMPLATE_GATEWAY_URL`, `VITE_AGENT_NATIVE_USE_TEMPLATE_GATEWAY`, `VITE_AGENT_NATIVE_WORKSPACE`, `VITE_AGENT_NATIVE_WORKSPACE_APP_ID`, `VITE_APP_BASE_PATH`, `VITE_APP_NAME`, `VITE_APP_TEMPLATE`, `VITE_APP_URL`, `VITE_WORKSPACE_GATEWAY_URL`, `VITE_WORKSPACE_OAUTH_ORIGIN`, `WORKSPACE_GATEWAY_URL`, `WORKSPACE_OAUTH_ORIGIN`, `WORKSPACE_PORT`, `WORKSPACE_SECRETS_ENCRYPTION_KEY`, `WORKSPACE_SECRETS_ENCRYPTION_KEY_PREVIOUS`

### Application, authentication, and secrets

`ACCESS_TOKEN`, `ACCESS_TOKENS`, `AIR_HOST`, `ALLOW_DRIZZLE_PUSH_ON_NEON`, `AMPLITUDE_API_KEY`, `AN_ALLOW_PG_DEV_OWNER_RESET`, `APP_BASE_PATH`, `APP_ID`, `APP_NAME`, `APP_TEMPLATE`, `APP_URL`, `APPDATA`, `AUTH_DISABLED`, `AUTH_MAGIC_LINK`, `AUTH_MODE`, `AUTH_SKIP_EMAIL_VERIFICATION`, `AUTO_CREATE_DEFAULT_ORG`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `BRANCH`, `BUILDER_ADMIN_API_HOST`, `BUILDER_API_HOST`, `BUILDER_APP_HOST`, `BUILDER_BRANCH_PROJECT_ID`, `BUILDER_DESIGN_SYSTEMS_BASE_URL`, `BUILDER_GATEWAY_BASE_URL`, `BUILDER_HOST`, `BUILDER_IMAGE_GENERATION_BASE_URL`, `BUILDER_IS_ENTERPRISE`, `BUILDER_IS_FREE_ACCOUNT`, `BUILDER_ORG_KIND`, `BUILDER_ORG_NAME`, `BUILDER_PRIVATE_KEY`, `BUILDER_PROJECT_ID`, `BUILDER_PROXY_ORIGIN`, `BUILDER_PUBLIC_APP_HOST`, `BUILDER_PUBLIC_KEY`, `BUILDER_SUBSCRIPTION`, `BUILDER_SUBSCRIPTION_LEVEL`, `BUILDER_SUBSCRIPTION_NAME`, `BUILDER_USER_ID`, `BUILDER_WEB_SEARCH_BASE_URL`, `CONTEXT`, `COOKIE_DOMAIN`, `CORS_ALLOWED_ORIGINS`, `DATABASE_AUTH_TOKEN`, `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `DB_OP_TIMEOUT_MS`, `DEBUG`, `DEBUG_CREDENTIAL_RESOLVE`, `DISPATCH_ALLOW_DEFAULT_OWNER_APP_CREATION`, `DISPATCH_BUILDER_PROJECT_ID`, `DISPATCH_DEFAULT_OWNER_EMAIL`, `DISPATCH_TRUST_UNVERIFIED_EMAIL_SENDER`, `DO_NOT_TRACK`, `ENABLE_BUILDER`, `FUSION_ENV_ORIGIN`, `FUSION_ENVIRONMENT`, `GH_TOKEN`, `GIT_COMMIT_SHA`, `INTEGRATION_RUN_RESERVATION_MICROS`, `OAUTH_STATE_SECRET`, `OWNER_EMAIL`, `PING_MESSAGE`, `SECRETS_ENCRYPTION_KEY`, `VITE_BETTER_AUTH_URL`, `VITE_FUSION_ENV_ORIGIN`, `VITE_POSTHOG_HOST`, `VITE_POSTHOG_KEY`, `VITE_POSTHOG_PUBLIC_KEY`, `VITE_SENTRY_CLIENT_DSN`, `VITE_SENTRY_CLIENT_KEY`, `VITE_SENTRY_DSN`, `VITE_SENTRY_INGEST_HOST`, `VITE_SENTRY_PROJECT_ID`, `WEBHOOK_BASE_URL`

### Integrations and observability

`ANTHROPIC_API_KEY`, `BRAVE_SEARCH_API_KEY`, `COHERE_API_KEY`, `DESIGN_ACCESS_TOKEN`, `DESIGN_APP_URL`, `DISCORD_APPLICATION_ID`, `DISCORD_PUBLIC_KEY`, `EMAIL_AGENT_ADDRESS`, `EMAIL_FROM`, `EMAIL_INBOUND_WEBHOOK_SECRET`, `EXA_API_KEY`, `FIGMA_ACCESS_TOKEN`, `FIRECRAWL_API_KEY`, `GA_MEASUREMENT_ID`, `GEMINI_API_KEY`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `GITHUB_REPOSITORY`, `GITHUB_TOKEN`, `GOOGLE_APPLICATION_CREDENTIALS`, `GOOGLE_AUTH_MODE`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_DOCS_PUSH_AUDIENCE`, `GOOGLE_DOCS_PUSH_SIGNER_EMAIL`, `GOOGLE_GENERATIVE_AI_API_KEY`, `GOOGLE_LEGACY_CLIENT_ID`, `GOOGLE_LEGACY_CLIENT_SECRET`, `GOOGLE_SERVICE_ACCOUNT_KEY`, `GOOGLE_SIGN_IN_CLIENT_ID`, `GOOGLE_SIGN_IN_CLIENT_SECRET`, `GROQ_API_KEY`, `GTM_CONTAINER_ID`, `MICROSOFT_TEAMS_ALLOWED_TENANT_IDS`, `MICROSOFT_TEAMS_APP_ID`, `MICROSOFT_TEAMS_APP_PASSWORD`, `MICROSOFT_TEAMS_APP_TENANT_ID`, `MISTRAL_API_KEY`, `MIXPANEL_TOKEN`, `NOTIFICATIONS_EMAIL_RECIPIENTS`, `NOTIFICATIONS_SLACK_WEBHOOK_AUTH`, `NOTIFICATIONS_SLACK_WEBHOOK_URL`, `NOTIFICATIONS_WEBHOOK_AUTH`, `NOTIFICATIONS_WEBHOOK_URL`, `OPENAI_API_KEY`, `OPENAI_BASE_URL`, `OPENROUTER_API_KEY`, `POSTHOG_AI_FEEDBACK_SURVEY_ID`, `POSTHOG_AI_FEEDBACK_SURVEY_QUESTION_ID`, `POSTHOG_API_KEY`, `POSTHOG_ERROR_TRACKING`, `POSTHOG_HOST`, `POSTHOG_PUBLIC_HOST`, `POSTHOG_PUBLIC_KEY`, `PYLON_API_BASE_URL`, `PYLON_API_KEY`, `RESEND_API_KEY`, `SENDGRID_API_KEY`, `SENDGRID_FROM_EMAIL`, `SENTRY_CLIENT_DSN`, `SENTRY_CLIENT_KEY`, `SENTRY_DEBUG`, `SENTRY_DESKTOP_CLIENT_KEY`, `SENTRY_DESKTOP_DEBUG`, `SENTRY_DESKTOP_DSN`, `SENTRY_DESKTOP_ENABLED`, `SENTRY_DESKTOP_ENVIRONMENT`, `SENTRY_DESKTOP_INGEST_HOST`, `SENTRY_DESKTOP_PROJECT_ID`, `SENTRY_DESKTOP_RELEASE`, `SENTRY_DSN`, `SENTRY_ELECTRON_CLIENT_KEY`, `SENTRY_ELECTRON_DEBUG`, `SENTRY_ELECTRON_DSN`, `SENTRY_ELECTRON_ENABLED`, `SENTRY_ELECTRON_ENVIRONMENT`, `SENTRY_ELECTRON_INGEST_HOST`, `SENTRY_ELECTRON_PROJECT_ID`, `SENTRY_ELECTRON_RELEASE`, `SENTRY_ENVIRONMENT`, `SENTRY_INGEST_HOST`, `SENTRY_PROJECT_ID`, `SENTRY_RELEASE`, `SENTRY_SERVER_DSN`, `SENTRY_SERVER_TRACES_SAMPLE_RATE`, `SLACK_BOT_TOKEN`, `SLACK_CLIENT_ID`, `SLACK_CLIENT_SECRET`, `SLACK_SIGNING_SECRET`, `TAVILY_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, `TRACKING_WEBHOOK_AUTH`, `TRACKING_WEBHOOK_URL`, `TURNSTILE_SECRET_KEY`, `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_APP_SECRET`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_VERIFY_TOKEN`

### Build, host, and local tooling

`AWS_EXECUTION_ENV`, `AWS_LAMBDA_FUNCTION_MEMORY_SIZE`, `AWS_LAMBDA_FUNCTION_NAME`, `AWS_REGION`, `CF_PAGES`, `CF_PAGES_BRANCH`, `CF_PAGES_COMMIT_SHA`, `CHOKIDAR_INTERVAL`, `CHOKIDAR_USEPOLLING`, `CI`, `CLAUDE_CODE_SESSION_ID`, `CLAUDE_CONFIG_DIR`, `CLOUDFLARE_WORKERS`, `CODE_AGENTS_PROJECT_ROOT`, `CODEX_HOME`, `COMMIT_REF`, `COREPACK_HOME`, `DEPLOY_ID`, `DEPLOY_PRIME_URL`, `DEPLOY_URL`, `DEV`, `ELECTRON_RENDERER_URL`, `EXPO_ACCESS_TOKEN`, `FLY_APP_NAME`, `FLY_BRANCH`, `FORCE_COLOR`, `FRAME_PORT`, `FRAME_SERVER_PORT`, `FUNCTION_NAME`, `FUNCTIONS_WORKER_RUNTIME`, `HEAD`, `HOME`, `INIT_CWD`, `K_SERVICE`, `LAMBDA_TASK_ROOT`, `LC_ALL`, `LC_CTYPE`, `NETLIFY`, `NETLIFY_COMMIT_REF`, `NETLIFY_CONTEXT`, `NETLIFY_DATABASE_AUTH_TOKEN`, `NETLIFY_DATABASE_URL`, `NETLIFY_DATABASE_URL_UNPOOLED`, `NETLIFY_LOCAL`, `NETLIFY_SITE_NAME`, `NITRO_DEV_RUNNER`, `NITRO_PRESET`, `NO_COLOR`, `NODE_ENV`, `NODE_OPTIONS`, `PATH`, `PI_CODING_AGENT_DIR`, `PLAN_BASE_URL`, `PLAN_BLOCKS_APP_URL`, `PLAN_LOCAL_APP_URL`, `PLAN_LOCAL_BRIDGE_APP_URL`, `PLAN_LOCAL_DIR`, `PLAN_PUBLISH_CONFIG_PATH`, `PLAN_RECAP_APP_URL`, `PLAN_RECAP_TOKEN`, `PLAN_VISUAL_ANSWER_APP_URL`, `PLAN_VISUAL_ANSWER_TOKEN`, `PNPM_HOME`, `PORT`, `PWD`, `RENDER`, `RENDER_GIT_BRANCH`, `SHELL`, `SITE_ID`, `SITE_NAME`, `SSR`, `URL`, `VERCEL`, `VERCEL_DEPLOYMENT_ID`, `VERCEL_ENV`, `VERCEL_GIT_COMMIT_REF`, `VERCEL_GIT_COMMIT_SHA`, `VERCEL_PROJECT_PRODUCTION_URL`, `VERCEL_REGION`, `VERCEL_URL`, `VITEST`, `XDG_CONFIG_HOME`

</details>

The owning feature guide is authoritative for an advanced variable's default,
accepted values, and security trade-offs. Start with [Security](/docs/security)
for opt-ins that affect credential or request handling.

## Per-app values in a workspace

The workspace root `.env` is loaded into every app. An app-local file such as
`apps/mail/.env` wins when the same key exists in both places, so use the root
file for shared values and the app file for app-specific values.

Some deploy-level values also support an app-name prefix. Set `APP_NAME`,
uppercase it, and replace non-alphanumeric characters with `_`:

```bash
APP_NAME=mail
MAIL_DATABASE_URL=<MAIL_DATABASE_URL>
MAIL_DATABASE_AUTH_TOKEN=<MAIL_DATABASE_AUTH_TOKEN>
MAIL_SECRETS_ENCRYPTION_KEY=<MAIL_SECRETS_ENCRYPTION_KEY>
```

The prefix form is supported for database URLs/tokens and app-local secret
encryption. Do not invent a prefix for an arbitrary variable; use
`apps/<name>/.env` or the shared root `.env` according to the variable's scope.

## Public versus secret values

Variables whose names start with `VITE_` and values read through
`import.meta.env` can be bundled into browser code. Treat them as public
configuration. Never put an API key, token, password, or private key in a
browser-exposed variable.

The exact index includes integration and host-detection keys because they are
hard-coded in first-party runtime code. They are not a setup checklist: use a
scoped workspace connection for user or provider credentials, and follow the
owning app or feature guide when one of these advanced keys is actually needed.
See [Security](/docs/security) for the credential boundary and
[Deployment](/docs/deployment) for production hosting.

## Keeping the reference complete

The maintainer-only repository inventory remains broader than this public page.
Run the focused check from the repository root when adding a framework variable:

```bash
pnpm run guard:env-documentation
```

The check keeps the exhaustive internal inventory aligned with source while
also checking that the curated framework contract above does not omit a key
first-party variable.
