---
"@agent-native/agentkit-react": minor
"@agent-native/agentkit": minor
"@agent-native/toolkit": minor
"@agent-native/core": patch
---

Add composable AgentKit React providers, hooks, conversation components, renderer
slots, streaming Markdown, and the public AgentKit facade. Share composer,
suggestion, queue, clipboard, and streaming helpers through Toolkit, and reuse
shared activity, approval, message action, and semantic styling components in
Core's existing chat surfaces. Validate the six-package graph through isolated
tarball installation, JavaScript and CSS imports, and a consumer build.
