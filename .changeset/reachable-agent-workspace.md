---
"@agent-native/core": patch
"@agent-native/migrate": patch
---

Keep the full Agent workspace reachable from chat-only sidebars, including generated migrated apps. The sidebar link now opens the Agent page where context, resources, connections, automations, and access are managed.
