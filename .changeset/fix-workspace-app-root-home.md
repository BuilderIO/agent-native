---
"@agent-native/core": patch
"@agent-native/dispatch": patch
---

Infer a root home route for workspace apps that do not define a `/home` route, and keep local and autonomous app discovery aligned with the deployed registry.
