---
"@agent-native/core": minor
---

Add strict, versioned E2EE protocol and resource-privacy contracts, including a hosted-field guard, a synthetic failure corpus, the frozen `anc/v1` cryptographic suite, deterministic canonical encoding, portable crypto wrappers, fixed interoperability vectors, and executable key-ceremony state machines.
