---
"@agent-native/core": minor
---

Create executable generic Code Agents sessions with transcript/follow-up support, make bare `agent-native` open the Code workspace, and emit migration dossiers without scaffolding the hidden migration app by default.
