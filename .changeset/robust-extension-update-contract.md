---
"@agent-native/core": patch
---

Prevent model gateways from breaking extension edits by using a compact required update contract and normalizing optional placeholder arguments before validation.
