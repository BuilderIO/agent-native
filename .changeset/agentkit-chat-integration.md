---
"@agent-native/core": patch
"@agent-native/dispatch": patch
---

Adopt AgentKit in the Chat template with focused Core composer and rail
entrypoints, trusted provider connection requests, OAuth continuation, guided
questions, and URL-backed conversations. Keep generated standalone and workspace
apps on compatible local or published packages, and stabilize Vite dependency
resolution, auth and application-state startup, SSR entry discovery, and route
recovery. Add standalone Chat and sign-in acceptance coverage. Update Dispatch
to use the shared prompt bar and conversation copy.
