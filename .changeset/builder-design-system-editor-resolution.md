---
"@agent-native/core": patch
"@agent-native/toolkit": patch
---

Resolve actual Builder project editor URLs separately from documentation links. Expose available editor URLs when indexing starts and through an access-scoped action, preserving typed lookup failures while projects are preparing.

Stage completed file-upload references alongside GitHub, code, and design.md sources, then index the batch in one agent action without passing binary file content through chat.

Add the shared shadcn Field primitive for standard selectable choice cards, including radio controls and optional tier or license badges. Document Field composition for setup flows such as Start fresh and Your references.
