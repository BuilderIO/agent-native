---
"@agent-native/core": minor
---

Allow deployment-operator actions to require an allowlisted session plus an
out-of-band step-up token, and allow sensitive actions to fail closed until
their audit receipt is durably stored.
