---
"@agent-native/core": patch
"@agent-native/dispatch": patch
---

Add a shared integration catalog with accurate built-in messaging metadata and reusable client helpers for integration setup routes.
