---
"@agent-native/core": patch
---

Treat the mounted deployment root marker as the app root so auth handoff works on unified Vercel deployments, and keep trusted server asset references paired with preview client artifacts.
