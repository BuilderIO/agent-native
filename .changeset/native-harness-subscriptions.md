---
"@agent-native/core": minor
---

Use AI SDK Harness native host subscription authentication for built-in harness adapters and remove the local Codex auth-file copy path. Existing `codexCliAuth` configuration can be removed.
