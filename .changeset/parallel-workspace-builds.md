---
"@agent-native/core": minor
---

Add `--concurrency <n|auto>` (and `AGENT_NATIVE_DEPLOY_CONCURRENCY`) to `agent-native deploy` so workspace app builds can run in parallel; builds stay sequential by default.
