---
"@agent-native/core": patch
"@agent-native/toolkit": patch
"@agent-native/agentkit": patch
---

Support searchable context menus and controlled context attachments across standalone, sidebar, and AgentKit composers, with readiness checks and immutable submission snapshots.

Keep context selection in one compact composer-anchored popover, with live rendered picker pages, inline search/back navigation, cancellation, and origin-preserving resume after creation dialogs.
