---
"@agent-native/core": minor
---

Add version-bound extension capability manifests, per-viewer accepted grants, server-side bridge enforcement, and immediate revocation with fail-closed legacy behavior.
