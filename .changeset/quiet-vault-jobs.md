---
"@agent-native/core": minor
---

Add a strict canonical semantic payload for encrypted broker jobs so native grant authorization can bind the exact resource, operation, and provider before local execution.
