---
"@agent-native/creative-context": patch
---

Let a host declare the role that recording generation provenance requires, so drafting in a resource you can only read no longer fails on its own provenance write.
