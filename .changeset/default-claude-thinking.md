---
"@agent-native/core": patch
---

Agent chat now requests adaptive thinking by default for capable Claude models when reasoning effort is left on "auto", so the Thinking cell streams by default instead of requiring an explicit effort selection.
