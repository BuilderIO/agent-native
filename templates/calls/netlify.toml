[build]
  command = "pnpm install && NITRO_PRESET=netlify pnpm --filter calls build"
  publish = "templates/calls/dist"
  functions = "templates/calls/.netlify/functions-internal"

[build.environment]
  NITRO_PRESET = "netlify"
