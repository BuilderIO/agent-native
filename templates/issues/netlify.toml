[build]
  command = "pnpm install && pnpm --filter issues exec drizzle-kit push --force && NITRO_PRESET=netlify pnpm --filter issues build"
  publish = "templates/issues/dist"
  functions = "templates/issues/.netlify/functions-internal"

[build.environment]
  NITRO_PRESET = "netlify"
