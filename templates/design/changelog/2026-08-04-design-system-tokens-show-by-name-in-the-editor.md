---
type: added
date: 2026-08-04
---

Design system tokens now show by name across the Brand Kit page and the fill, stroke, effects, page background and selection colour rows, so a colour reads as `cds-link-primary` instead of `0F62FE`.
