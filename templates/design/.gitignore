build
.output/

# `.generated/bridge` is committed source that DesignCanvas imports at module
# load — ignoring it yields a checkout where every design page 500s.
.generated/*
!.generated/bridge/

.vercel/
