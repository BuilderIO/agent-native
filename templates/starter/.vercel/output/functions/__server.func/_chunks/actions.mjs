import{a as e,r as t,t as n}from"./registry3.mjs";function r(e,t=20){let n=Number(e??t);return!Number.isFinite(n)||n<=0?t:Math.min(Math.floor(n),200)}function i(i){return{"manage-notifications":{tool:{description:[`Manage user notifications. Available actions:`,``,`• action="send" — Send a notification to the user. Persisted to the in-app inbox so the bell + toast surface shows it. Registered channels (webhook, Slack, etc.) also run.`,`  Required: severity, title. Optional: body, metadataJson, channels.`,``,`• action="list" — List recent notifications for the current user. Useful when the user asks about prior alerts.`,`  Optional: unreadOnly (boolean), limit (number, default 20, max 200).`].join(`
`),parameters:{type:`object`,properties:{action:{type:`string`,enum:[`send`,`list`],description:`The notification action to perform.`},severity:{type:`string`,enum:[`info`,`warning`,`critical`],description:`(send) Severity level — drives styling and per-severity channel routing. Use "info" for FYI, "warning" for things the user should look at, "critical" for things that need immediate attention.`},title:{type:`string`,description:`(send) Short, human-readable headline (≤100 chars).`},body:{type:`string`,description:`(send) Optional longer description.`},metadataJson:{type:`string`,description:`(send) Optional JSON metadata (URLs, entity ids, etc.). Example: '{"threadId":"abc","link":"/inbox/abc"}'.`},channels:{type:`string`,description:`(send) Optional comma-separated channel allowlist (e.g. "inbox,webhook"). Omit to run all registered channels.`},unreadOnly:{type:`boolean`,description:`(list) When true, only include unread notifications.`},limit:{type:`number`,description:`(list) Max rows to return (default 20, max 200).`}},required:[`action`]}},run:async a=>{let o=i(),s=a.action;switch(s){case`send`:{if(!a.severity||!a.title)return`Error: severity and title are required for action=send.`;let e=a.severity;if(![`info`,`warning`,`critical`].includes(e))return`Error: severity must be info, warning, or critical (got "${e}").`;let t;if(a.metadataJson)try{t=JSON.parse(a.metadataJson)}catch{return`Error: metadataJson must be valid JSON.`}let r=typeof a.channels==`string`?a.channels.split(`,`).map(e=>e.trim()).filter(Boolean):void 0,i=await n({severity:e,title:a.title,body:a.body||void 0,metadata:t,channels:r},{owner:o});return i?`Notification sent (id: ${i.id})`:`Notification dispatched to channels (not persisted).`}case`list`:{let n=await e(o,{unreadOnly:a.unreadOnly===!0||a.unreadOnly===`true`,limit:r(a.limit)});return n.length===0?a.unreadOnly?`No unread notifications.`:`No notifications.`:`${await t(o)} unread\n\n${n.map(e=>`[${e.readAt?` `:`•`}] (${e.severity}) ${e.title}${e.body?` — ${e.body}`:``} · ${e.createdAt}`).join(`
`)}`}default:return`Error: unknown action "${s}". Must be one of: send, list.`}}}}}export{i as createNotificationToolEntries};