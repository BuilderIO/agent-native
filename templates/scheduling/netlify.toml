[build]
  command = "export DATABASE_URL=${NETLIFY_DATABASE_URL:-$DATABASE_URL} && pnpm install && DATABASE_URL=${NETLIFY_DATABASE_URL_UNPOOLED:-$DATABASE_URL} pnpm --filter scheduling exec drizzle-kit push --force && NITRO_PRESET=netlify pnpm --filter scheduling build"
  publish = "templates/scheduling/dist"
  functions = "templates/scheduling/.netlify/functions-internal"

[build.environment]
  NITRO_PRESET = "netlify"
  NPM_CONFIG_PRODUCTION = "false"
