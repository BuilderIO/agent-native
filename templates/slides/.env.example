# Add your API keys here (or configure via Settings in the app)
# GOOGLE_API_KEY=
# GOOGLE_SEARCH_CX=

# Database — defaults to local SQLite (file:./data/app.db)
# Set to a remote URL for cloud database (e.g. libsql://your-db.turso.io)
# DATABASE_URL=
# DATABASE_AUTH_TOKEN=
