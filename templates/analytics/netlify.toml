[build]
  command = "pnpm install && NITRO_PRESET=netlify pnpm --filter analytics build"
  publish = "templates/analytics/dist"
  functions = "templates/analytics/.netlify/functions-internal"

[build.environment]
  NITRO_PRESET = "netlify"
