[build]
  command = "pnpm install && NITRO_PRESET=netlify pnpm --filter content build"
  publish = "templates/content/dist"
  functions = "templates/content/.netlify/functions-internal"

[build.environment]
  NITRO_PRESET = "netlify"
