import { messagesByLocale } from "../i18n-data";
import { pageTrashMessages } from "../page-trash-messages";

export default {
  ...messagesByLocale["en-US"],
  pageTrash: pageTrashMessages["en-US"],
};
