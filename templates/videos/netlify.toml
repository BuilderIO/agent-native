[build]
  command = "pnpm install && pnpm --filter videos exec drizzle-kit push --force && NITRO_PRESET=netlify pnpm --filter videos build"
  publish = "templates/videos/dist"
  functions = "templates/videos/.netlify/functions-internal"

[build.environment]
  NITRO_PRESET = "netlify"
