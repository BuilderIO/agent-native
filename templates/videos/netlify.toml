[build]
  command = "pnpm install && NITRO_PRESET=netlify pnpm --filter videos build"
  publish = "templates/videos/dist"
  functions = "templates/videos/.netlify/functions-internal"

[build.environment]
  NITRO_PRESET = "netlify"
