interface:
  display_name: "Ship Now"
  short_description: "Targeted prep, owned-path merge, then monitor"
  default_prompt: "Use $ship-now to run prep or targeted recovery, push only this agent's owned paths, admin-merge immediately without waiting for full CI or soak, rotate, and monitor the release tail."
